# Audit Round 14 — Full Static Audit (2026-07-04)

**Method**: 8 dimension-scoped finder agents over `index.html` (V231 tip, 24,736 lines) + adversarial verification of every finding (batched per dimension; refutation required a concrete guard line — "can't happen in practice" dismissals banned per repo rules).

**Result**: 46 findings, **46 CONFIRMED / 0 REFUTED / 0 UNCERTAIN** — 9 CRITICAL / 23 MEDIUM / 14 LOW.

**Caveats**: (1) Zero refutations across 46 findings — verifier reasonings cite specific traced lines, but a 100% confirm rate means the verify pass never disagreed with a finder; treat verdicts as strong-static-evidence, not runtime-proven. (2) Static analysis only — no MPStress runs; findings touching the pipeline should get targeted reproducers before/with fixes. (3) Excludes all R11–R13 closed/deferred items and documented design decisions.

**Not fixed**: no code was changed. Fix decisions are the user's (per repo rule: production changes surfaced before implementation).


## CRITICAL (9)

### C1. [pipeline-order] `index.html:14548` — Single-person randomize clears BOTH persons' freeze pins but re-freezes only the target person — non-target trip totals come off container boundaries, firing household INV20 (hard)

**Failure scenario**: User runs a shared Randomize (freezeTripTotals pins her pkg/produce amounts at container multiples via _freezePin overrides), then opens the randomize popup, unchecks Her, and runs him-only (runRandomizeFromPopup 13250-13255 → randomizeWeek('him'); same via applyScheduleEdit rTarget=person at 13564). _clearFreezeOverrides at 14548 (and 14623/14718 per retry) is UNSCOPED — it deletes her slots' _freezePin overrides too — and invalidateLeftoverCache rebuilds her days at raw budget-scaled, non-boundary amounts. But every freezeTripTotals call in the run passes persons=['him'] (10997), and _freezeOneItem iterates only those persons' anchor slots (11551), so her-anchored solos and batches are never re-rounded or re-pinned. verifyInvariants' INV20 check uses buildShoppingList(trip,'both') (7555) — household-wide — so it hard-fires on her items (e.g. her silken-tofu cooks now total 9.3oz vs a 14oz carton), and her shopping quantities materially revert to wasteful values even though the user explicitly asked to leave Her untouched ('her's stays', comment 13561-13563).

**Verdict (CONFIRMED)**: _clearFreezeOverrides iterates ALL OVERRIDES keys (3757-3769) — deletes her _freezePin entries on a him-only run (called 14548/14623/14718). freezeTripTotals derives persons=['him'] (10997) and _freezeOneItem collects contributors only from those persons' anchors (11550-11551), so her items are never re-rounded/re-pinned; invalidateLeftoverCache (14616/14627/14675/14720) rebuilds her days at budget-scaled non-boundary amounts (RBA snaps to ingredient grid, not container multiples). INV20 verify reads buildShoppingList(trip,'both') (7555) — household-wide — so her off-boundary non-soft items hard-fire. Him-only randomize is user-reachable: runRandomizeFromPopup 13252-13253, applyScheduleEdit rTarget=person 13564.

### C2. [pipeline-order] `index.html:15376` — Kcal-reroll swap commits never clear the slot's idx-based overrides — stale _fatBoost/_freezePin entries substitute or mis-pin ingredients in the swapped-in meal

**Failure scenario**: In randomizeWeek's post-retry sequence, _autoApplyFatBoosts (line 14734) writes idx-keyed overrides {idx, dbKey:'avocado oil', amt, scalable:false, _fatBoost:true} on lean days' breakfast/snack slots (breakfast/snack meals DO carry fatBoost: egg_scramble, protein_pancakes, yogurt_oat_bowl, yogurt_snack). The same day can also be >100 kcal off, so rerollKcalOffBreakfast (called at 14749 and 14774) or rerollKcalOffSnacks swaps that slot's SEL — commit at 15376/15208 writes SEL + ts but never calls clearOverrides (contrast the freeze swap path, which does: lines 11107/11360). rebuildBreakfastCache → applyOverrides (3911-3916) matches the stale override BY IDX ONLY and rewrites the NEW meal's ingredient at that idx: dbKey is replaced by the boost ingredient at the old meal's boosted amount, or appended if idx >= new recipe length. Worse at the LATE call site (14774-14775, after freeze Pass 1+2): freeze pins (11273/11286) carry only amt+scalable:false at an idx, so e.g. swapping snack cucumber_hummus_light (cucumber 2.0c pinned at idx 0) to yogurt_snack forces the new meal's idx-0 greek yogurt row to amt=2.0, scalable:false. The trial scoring runs ON the corrupted rebuild, so a corrupted candidate that happens to score well is committed: card, shopping list, and persisted OVERRIDES (autoSaveWeek) all show a meal with a silently substituted/mis-amounted ingredient until the next randomize's _clearFreezeOverrides. No hard INV catches it (INV2/5/23/25 all read the same corrupted cache).

**Verdict (CONFIRMED)**: Commit sites 15376-15381 / 15208-15215 write SEL+ts but never clearOverrides (freeze swap path does: 11107, 11360). Stale idx-keyed overrides exist on these slots: _autoApplyFatBoosts writes {dbKey,amt,scalable:false,_fatBoost} (3888-3892; egg_scramble breakfast has fatBoost, line 1401) and freeze pins write amt+scalable+_freezePin without dbKey (11273-11274) on snack produce/pkg solos. applyOverrides matches by idx only (3912-3921, append at 3936-3948), so the swapped-in meal's row is substituted or amt-pinned; trial scoring runs on the corrupted rebuild (15352-15356). No later clear: _clearFreezeOverrides only at 14548/14623/14718, all before 14734/14749/14774; OVERRIDES persisted (17133). Materially wrong amounts cooked/shopped.

### C3. [render-display] `index.html:9121` — Cross-person combined view matches rows by dbKey with first-match break — duplicate dbKeys render wrong batch amounts (sibling: renderSharedCard maps overwrite dupes at 8289)

**Failure scenario**: Rebuild block sums each row by scanning portions for FIRST dbKey match then breaks. Duplicate dbKeys reachable via addTempIngredient (no already-in-recipe guard, 10415) and onIngrSwap to an existing key (8198). Added 0.5c spinach row renders the recipe row's 1.26c -> user measures 2.52c vs batch's 2.26c; kcal label wrong. INV23/INV25 reconstruct per-index so cannot fire.

**Verdict (CONFIRMED)**: Rebuild block 9117-9126 sums each cookBal row by scanning bp.bal for the FIRST dbKey match then break (9120-9121). With duplicate dbKeys, both rows display the first occurrence's sum — the appended row renders the recipe row's amount. Duplicates are reachable: confirmTempIngredient (10415-10454) has no already-in-recipe guard and pushes a new cache row per portion (10464-10472); onIngrSwap's dropdown offers all keys (8198-8201, 8223). Path is live for crossPersonCook combined view (9100). Sibling at 8289: himOneByKey/herOneByKey overwrite dupes (bucket totals at 8303-06 are correct, but per-person reference amts wrong). CRITICAL, data-dependent: wrong batch amounts on the cooking surface.

### C4. [render-display] `index.html:10637` — onIngrAmt ignores perServingMode — edits in per-serving view of a Big Cook anchor are interpreted as batch totals, shrinking all portions ~N-fold

**Failure scenario**: buildMealCardHtml (9024-32) sets cookServings=1 in perServingMode so dropdown values are per-serving; onIngrAmt never consults perServingMode (refs only at 2686/9024/9191/9813-18/pageState). 3-portion batch, 8oz/serving (batch 24): user in per-serving view picks 9 -> scale=9/24=0.375 -> every portion pinned at 3oz via _writeUserPin (_userSet bypasses INV13/unify); autoSaveWeek persists + syncs. No invariant fires.

**Verdict (CONFIRMED)**: Grep confirms perServingMode is referenced only at 2686/9024/9817-18/18456/18466 — never in onIngrAmt (10622-10699). In per-serving view, buildMealCardHtml sets cookServings=1 (9025, 9032), so rows at 9046-48 show per-serving amounts and the 9100 rebuild is skipped (!wantsPerServing). onIngrAmt's showCombined (10637) is true for any non-shared multi-portion anchor regardless of view, so the per-serving pick is divided by currentCombined (10652) and all portions are pinned at cur*scale via _writeUserPin (10656), which sets _userSet (10573) bypassing INV13/unify; autoSaveWeek persists (10691). Batch shrinks ~N-fold with no invariant fire. CRITICAL: materially wrong cooked/shopped amounts.

### C5. [shopping-sync] `index.html:17638` — mergeSyncData never invalidates the balanced/leftover caches — Pull renders and shops stale ingredient amounts

**Failure scenario**: Merge replaces weekData sel/overrides + loadWeekState, but no invalidateLeftoverCache anywhere in merge chain (only sync-region call site is in switchWeek at 17225); pullSync then calls renderMeals which by design does not invalidate. getDayBalancedIngredients (5512) returns cached pk(p,d) with no SEL fingerprint -> cards show new meal title with old ingredients; buildShoppingList sums stale amounts. INV2/INV5-violation-in-waiting that never fires (verifyInvariants only runs post-randomize). V143 bug class via sync.

**Verdict (CONFIRMED)**: mergeSyncData (17313-17653) mutates weekData sel/overrides/skipped and calls loadWeekState (17638) — loadWeekState (17170-17185) only copies maps, no invalidation. pullSync (17772-17773) then calls renderMeals, which has no invalidateLeftoverCache call site between 9245-9880. getDayBalancedIngredients returns cached pk(p,d) unconditionally (5511-5512). Helpers _syncEatOutsToSharedSchedule (5086) / _resolveStateContradictions (5167) / injectCustomMeals (17875) don't invalidate either (verified against full call-site list). The V143 comment (17215-17225) documents this exact stale-name/old-ingredients symptom but fixed only switchWeek. Cards + buildShoppingList serve pre-merge amounts; verifyInvariants runs only post-randomize so INV2/INV5 never fire. Materially wrong amounts shown and shopped.

### C6. [ui-state-overrides] `index.html:10810` — onIngrReset restores a stale _postRandomizeCache snapshot with no meal-id check — after changeMeal, Reset writes the OLD meal's ingredients under the NEW meal's card

**Failure scenario**: Randomize runs → _capturePostRandomizeSnapshot (5437) stores meal A's balanced list for him_Tuesday dinner. User picks meal B via the card dropdown (changeMeal 10146 — updates SEL, rebuilds cache; _postRandomizeCache is never updated or invalidated: its only writers are 5438 and the week-switch clear at 17226). User edits one ingredient of meal B (_writeUserPin → _hasUserOverrides true → ↺ Reset button renders, 9192), then taps Reset. onIngrReset line 10810 finds the snapshot entry and at 10812 copies meal A's ingredient array into the live _dayBalancedCache — there is no comparison of the snapshot against the slot's current SEL (snapshot entries carry no meal id). Result: the card titled meal B renders meal A's ingredient rows and amounts, and calcTotals/buildShoppingList/computeCardMacros all read the same cache, so the stats bar and shopping list follow the wrong meal. No invariant catches it (INV2/INV5/INV25 compare display↔cache, which are consistently wrong together). The user shops and cooks meal A's amounts under meal B's name until the next full cache invalidation. Reset also deletes MANUAL_SET (10840), un-locking the user's explicit meal pick.

**Verdict (CONFIRMED)**: _postRandomizeCache is written only at post-randomize capture (5437-5441), week-switch clear (17226), and MPStress restore (18608-18610). changeMeal (10146-10192) stamps SEL, clearOverrides, invalidateLeftoverCache — never invalidates the snapshot, whose entries are slot-keyed ingredient arrays with no meal id (5439-5440). onIngrReset restores at 10810-10812 with only an existence check; Reset button renders whenever _hasUserOverrides (9192, 3721-3727). It writes _dayBalancedCache directly, deletes MANUAL_SET (10840), and calls renderMeals (10867) without invalidation, so getDayBalancedIngredients serves the old meal's rows to calcTotals/buildShoppingList/computeCardMacros under the new meal's title. No guard found; CRITICAL stands.

### C7. [ui-state-overrides] `index.html:15956` — Phase-1 shared-sync overwrites her SEL without clearOverrides — orphaned _userSet pins apply the OLD meal's pinned amount to the NEW meal, silently

**Failure scenario**: Producer: her Mon dinner has a user pin (e.g. idx-0 amt 6 for 6 oz salmon; OVERRIDES entry has _userSet:true, scalable:false) and its MANUAL_SET is later cleared while the override survives (toggleSkip ON→OFF at 9848-9854, toggleEatOut OFF at 10017, _v70UndoEatOutSide 3356, or _v85ClearSideVisualIfAny 3455 — all delete MANUAL_SET/USER_EDITED but never touch OVERRIDES). SHARED_SCHEDULE['Monday_dinner']='shared'. User clicks Randomize (target shared): _clearFreezeOverrides (3762) deliberately preserves _userSet entries; her slot is not locked (isLockedForRandomize=EAT_OUT||MANUAL_SET only, line 2748); him's Phase-1 pass then runs the shared-sync at 15946-15959 which sets SEL[her_Monday_dinner]=himId AND locked=true BEFORE her's person loop — so the clearOverrides at 15929 (which every other SEL-overwrite site pairs with: 15929, 13692, 10320, 10176) never runs for her slot. On rebuild, applyOverrides (3916) applies the orphan pin at idx 0 of the NEW meal — e.g. beef stew idx 0 = olive oil → 6 tbsp olive oil per serving, pinned scalable:false. Adjuster, unify, INV7 (comment line 14094: 'INV7 already exempts _userSet items') and INV13 all bypass user pins, so no invariant fires: the card, calcTotals, and the shopping list silently carry ~540 kcal of wrong ingredient with no Set pill explaining it.

**Verdict (CONFIRMED)**: Full chain verified. Producers exist: toggleSkip OFF (9848-9854), toggleEatOut OFF (10015-10030), _v70UndoEatOutSide (3350-3357), _v85 manual-purple branch (3453-3456) all delete MANUAL_SET but never touch OVERRIDES. _clearFreezeOverrides preserves _userSet (3762). locked map = isLockedForRandomize (15710, 2748) so the slot is unlocked; him's shared-sync (15949-15958) writes SEL[her]+locked=true with NO clearOverrides, and her Phase-1 pass returns at 15928 before clearOverrides (15929). applyOverrides (3911-3925) applies the orphan amt/scalable/_userSet at the same idx of the NEW meal; INV13 skips _userSet idxs (7336-7344), INV7 skips at 7094. Materially wrong amounts shown/shopped with no invariant or Set pill.

### C8. [unify-freeze-waste] `index.html:11146` — Freeze-swap co-contributor cleanup (_clearIngOverride / _cClearIng 11394) splices out _userSet override entries — user pins destroyed on ACCEPTED swaps

**Failure scenario**: Removes every override at matched idx, no _userSet check, no MANUAL_SET slot skip — unlike _clearFreezeOverrides (3762) which preserves _userSet. Swap of an unrelated solo whose outgoing meal shares a ROUND_PRODUCE ingredient walks all trip slots and splices the user's pinned bell-pepper entry on her MANUAL_SET lunch; V175 snapshot restores only on verify-FAIL, so accepted swap silently reverts the user edit.

**Verdict (CONFIRMED)**: _clearIngOverride splices every override whose idx matches, no _userSet or MANUAL_SET guard (11141-11147); combined-week twin _cClearIng identical (11390-11397). Cleanup walks ALL trip slots/persons/slots (11156-11188) plus batch portions (11180-11184) for any ROUND item shared with the outgoing meal (11155). V175 snapshot (11143) is only restored on verify-FAIL (11216-11224); the accepted path breaks out keeping the mutation (11196-11214). _trySwapForWaste's MANUAL_SET guard (11802) protects only the swap target, not co-contributor slots. Contrast _clearFreezeOverrides which preserves _userSet (3760-3763). Accepted swap silently destroys a user pin = data loss.

### C9. [unify-freeze-waste] `index.html:11266` — freezeTripTotals scales user-pinned contributors in its plan but skips their write — trip total lands off the container boundary (hard INV20)

**Failure scenario**: _freezeOneItem collects ALL contributors (no _userSet check, 11548-92); _tryFreezeY scales every portion by Y/tripTotal (11976); V163 write-skip only holds on the _onBoundary fast path. Pinned 9oz tofu + 8oz solo, perUnit 14: plan 7+7, write skips pin -> actual 9+7=16 -> 2 containers, 43% waste -> per-trip INV20+INV21 hard fire; freeze reported success so the swap fallback never runs; Pass 2/3 repeat the divergence. Correct plan (pin fixed, other->5) exists.

**Verdict (CONFIRMED)**: _freezeOneItem collects contributors with no _userSet check (11548-11592, solo push 11567); _tryFreezeY scales ALL solos/portions by Y/tripTotal (11959, 11976-11981, 12021-12023). Write sites skip pinned entries via _isUserPinned (11266, 11279; helper 11044-11051). So the plan assumes the pin moves to its scaled/snapped value but the write leaves it at the user amount — actual trip total = Y - plannedPin + actualPin, off the container boundary. _freezeOneItem returned a plan (non-null), so the attemptSwap fallback at 11059-11060 never runs. Grid quantization keeps later passes from fully converging. Hard INV20/INV21 fire + extra container shopped. Data-dependent only on a user pin on a ROUND item, which _clearFreezeOverrides preserves (3762).


## MEDIUM (23)

### M1. [adjuster-balancer] `index.html:1133` — Egg whole -> egg white auto-swap: a full swap (swaps === wholeAmt) is silently undone by the wholeOnly snap floor, leaving fat% over limit while the egg-white addition sticks

**Failure scenario**: swaps = Math.min(Math.ceil(excessFatCal/fatSaved), wholeAmt) allows zeroing egg whole, and the swap does NOT set fatDropped for it. The snap pass then hits the wholeOnly branch (lines 1160-1163): Math.round(0)=0 -> 'if (amt < 1) amt = 1' resurrects the egg (minAmt:1 at line 1199 and the final sweep would do the same). Concrete: egg_scramble breakfast (1 egg whole scalable, 0.75c egg white, avocado-oil fatBoost pinned scalable:false so Step-2's drop loop has no droppable fat candidates) scaled to uniformScale<0.85 with fat% still > maxFatPct -> swaps = min(1,1) = 1 -> adjustments[egg whole]=0, egg white += 0.125c. Snap restores egg whole to 1. Net output: the meal keeps its whole egg (zero fat removed, so fat% stays over the 30% limit — the exact condition the swap exists to fix per CLAUDE.md 'Egg wholes auto-swap to egg whites when fat % exceeds limit') PLUS a phantom +0.125c egg white (+16.6 kcal / +3.3 g pro) that was compensation for a swap that never happened.

**Verdict (CONFIRMED)**: Line 1133 allows swaps===wholeAmt; line 1135 sets adjustments[eggWholeIdx]=0 with NO fatDropped tag (only the Step-2 drop loop at 1115 sets it). 'egg whole' is wholeOnly:true, minAmt:1 (line 400), so the snap branch 1160-1163 (Math.round(0)=0; amt<1→1) resurrects it; line 1199 (minAmt, !fatDropped) would too. The egg-white increment at 1138 (+swaps×0.125c, 133 kcal/cup → ~16.6 kcal, 3.3g pro per swap) is never reverted. Net for a full swap: zero fat removed (fat% stays over maxFatPct — the swap's purpose) plus phantom egg white. Partial swaps (wholeAmt>swaps) survive snap correctly, so only the full-swap case breaks. MEDIUM stands.

### M2. [adjuster-balancer] `index.html:1334` — adjustIngredients final min/max sweep resurrects fat-dropped (zeroed) ingredients at minAmt, undoing Step-2 fat reduction after backfill already refilled its calories

**Failure scenario**: Step 2 zeroes the worst fat contributor and tags it in fatDropped; the snap pass (line 1157) and the minAmt recheck (line 1199) both honor that tag, but the final sweep (lines 1315-1339) has no fatDropped guard and, unlike the INV13 verifier (which skips amt<0.001 as 'effectively absent'), clamps the 0 back up to _effMin. Concrete: her solo lunch = burrito_bowl (I('avocado',0.5,'fat') scalable), budget ~540 vs base ~700 kcal -> uniformScale ~0.77 (<0.85) and fat% >33% -> Step 2 sets avocado to 0 (fatDropped). Backfill (lines 1207-1241) sees postSnapKcal WITHOUT the avocado and refills the gap with carb/protein; trim sees overBy~0. The final sweep then returns avocado at minAmt 0.25 each = +80 kcal / +7.5 g fat that no later accounting sees. The slot leaves adjustIngredients ~80 kcal over its effective budget with fat% back above the 30% limit the drop was enforcing (the drop's kcal was both backfilled AND partially restored). For dinner there is no per-slot over-budget enforcement, so the +80 persists into the day total; P0 only corrects |gap|>100.

**Verdict (CONFIRMED)**: fatDropped set at 1114-1115; snap pass honors it (1157) and minAmt recheck honors it (1199), but the final sweep (1315-1339) does not — line 1334 clamps the zeroed amt up to _effMin. Avocado (line 500) has minAmt:0.25, role 'fat' (lines 1792/1826), scalable:true during the pipeline, not in ROUND lists, so skips at 1317/1320/1327 don't apply. Backfill (1207-1241) and trim (1246-1298) both run BEFORE the sweep and honor fatDropped, so the resurrected +80 kcal/+7.5g fat lands after all budget accounting. Day-balancer P0 only corrects |gap|>100 (line 6085). No hard INV fires (cache stays internally consistent) — MEDIUM stands.

### M3. [adjuster-balancer] `index.html:5999` — balanceDayMacros P4 veg boost (boostFV) mutates user-pinned (_userSet) ingredients — violates the documented 'user edits are absolute / pins frozen' contract

**Failure scenario**: bestAdd/bestTrim use itemsByRole(role) which skips scalable:false (so user pins are safe there), but boostFV calls itemsByRole(role, true) to deliberately bypass scalable:false and its filter (lines 5991-6011) only excludes ROUND_PKG_ITEMS/ROUND_PRODUCE_ITEMS freeze pins — there is no _userSet check anywhere in the path (itemsByRole doesn't even forward the flag), and applyDelta writes slots[slot][idx].amt directly. Concrete: user edits lettuce (role veg, NOT in ROUND_PRODUCE_ITEMS; same exposure for brussels sprouts, green beans, frozen corn, bean sprouts) on a solo taco-salad lunch from recipe 1.5c down to 0.5c (_userSet:true, scalable:false), then taps Re-balance (or any action that invalidates and rebuilds the day cache). Day veg < 2.75c -> P4 sorts candidates by kcal/cup, picks lettuce (10 kcal/cup, typically cheapest available) and grows it 0.25c per pass up to min(3x recipeAmt=4.5, maxAmt=3) = 3c. Card and shopping list show 3c against the user's typed 0.5c; INV13 skips the row (user pin) so nothing fires. Contradicts CLAUDE.md/system_user_edit_architecture: 'Adjuster/unify/snap/INV13 all bypass _userSet pins' and Re-balance = 'full pipeline rerun with user pins frozen'.

**Verdict (CONFIRMED)**: boostFV calls itemsByRole(role, true) — line 5845 skips scalable:false only when !ignoreScalableFalse, so user pins pass through. itemsByRole doesn't forward _userSet (line 5849 field list), and boostFV's filter (5991-6011) excludes only scalable:false items in ROUND_PKG/PRODUCE_ITEMS (5999-6001). Lettuce (line 459: veg, cup, minAmt:0.5, maxAmt:3) is NOT in ROUND_PRODUCE_ITEMS (line 10968). P4 (6279-6282) applies via applyDelta, which writes slots[slot][idx].amt directly (6075). bestAdd/bestTrim use itemsByRole(role) without the flag so pins are safe there — boostFV is the sole hole. Contradicts the documented '_userSet pins are absolute' contract. MEDIUM (user edit silently overridden, recoverable via re-edit/Reset).

### M4. [inv-checker-and-db] `index.html:6859` — INV23 gate omits same-person servings>1 combined views AND verifies synthetic anchorxN rows instead of the rebuilt per-portion rows actually rendered

**Failure scenario**: Render gate (8962) is servings>1 || crossPersonCook; INV23 requires crossPersonCook -> most common Big Cook shape (same-person batch) never checked at xN. On the covered branch, buildMealCardHtml REPLACES rows with true per-portion sums (9100-28) which differ from anchor-bal x N whenever portions are kcal-prop unequal (always) -> on-screen contract violated invisibly. Found independently by two finders.

**Verdict (CONFIRMED)**: Gate mismatch is real: INV23 (6859) requires crossPersonCook; render gate cardShowsCombinedHeader (8962) is servings>1 || crossPersonCook, so same-person batches render combined ×N (9032) but are checked at cookServings23=1. On the covered crossPersonCook branch, buildMealCardHtml replaces rows at 9100-9127 with combinedAmt = Σ per-portion bal amounts, while INV23 (6870-6874) verifies synthetic anchor-bal × N rows; these differ whenever portions are kcal-prop unequal (him 2800 vs her 1900 budgets — always). INV23 thus never inspects what is rendered on that branch; the documented row-sum=header×N contract does not hold for the actual screen. Coverage gap on a hard invariant = false assurance; MEDIUM.

### M5. [inv-checker-and-db] `index.html:6996` — INV25 is tautological — buildQtyOptions unconditionally appends the exact current amount, so the matched-loop can never fail

**Failure scenario**: buildQtyOptions (768-79) pushes {val:currentAmt} when no grid option within 0.02 and INV25 skips amt<0.001 -> matched always true. Zero live coverage of the documented round-trip contract; cannot catch the 9121 duplicate-dbKey render bug or any render-passes-non-cache-amount regression. Dead-invariant pattern.

**Verdict (CONFIRMED)**: Tautology traced: INV25 skips amt<0.001 (6988), so displayAmt>0. It calls buildQtyOptions(unit, displayAmt, ...) at 6991; buildQtyOptions (768-779) checks for an option within 0.02 of currentAmt and, if none, pushes {val:currentAmt} whenever currentAmt>0. Therefore an option within 0.02 of displayAmt always exists and the matched loop (6996-6997, same 0.02 tolerance) can never fail — INV25 cannot fire for any reachable input. It also never inspects rendered HTML, so it cannot catch a render passing a non-cache amount (e.g., the 9117-9126 rebuilt rows). Dead hard invariant giving false 'all 0' assurance on the cooking surface; MEDIUM per project dead-invariant precedent.

### M6. [inv-checker-and-db] `index.html:20321` — saveBaseline hardFail filter missing the V207 INV20Leftover exclusion formatReport got — corrupts saved stress baselines

**Failure scenario**: invTotals includes INV20Leftover (init 18901) which fires routinely (7792) -> saveBaseline stores hardFail=1 on a clean run -> every later baseline comparison renders a false hard-INV delta.

**Verdict (CONFIRMED)**: saveBaseline's hardFail filter (20317-20322) excludes INV6/14/15/16/18/INV20Soft/21/22 and special-cases INV20, but omits 'INV20Leftover'. agg.invTotals initializes the key at 18901 and the aggregator counts fires (18728); verifyInvariants pushes 'INV20Leftover forward-inventory' at 7792 whenever a crossTripCarry item leaves a cook2 partial >0.05 — routine for marinara/broth (the flag exists because this happens). formatReport's hardFail got the V207 exclusion (19866-19871) but saveBaseline did not, so a clean run saves hardFail=1; the baseline table at 19921 then renders '1 fail' vs 'all 0 ✓' — a false hard-INV delta in every later comparison. MEDIUM (false signal, recoverable by resaving).

### M7. [leftover-detector] `index.html:4104` — _collectHouseholdCooks counts SKIPPED/EAT_OUT slots (incl. DEFAULTS fallback) as cooks — phantom INV14 fires + reroll churn

**Failure scenario**: No SKIPPED/EAT_OUT gate, unlike INV11 collector (7222) and computeLeftovers (4006). Two scenarios: (a) eat-out slot with no SEL falls back to DEFAULTS via getMealId — a legit pick of that meal elsewhere fires INV14 on a slot where nothing is cooked; (b) skipped slot retains SEL (toggleSkip 9847 never touches SEL) and user re-cooks that meal 2 days later — INV14 fires gap=2 though cooked once; tryResolveCook refuses EAT_OUT/SKIPPED/MANUAL_SET slots (15596-7) so the fire survives every randomize.

**Verdict (CONFIRMED)**: _collectHouseholdCooks (4099-4112) has no SKIPPED/EAT_OUT gate and uses getMealId (3962: SEL || DEFAULTS fallback), unlike computeLeftovers (4006) and the INV11 collector (7222). All three INV14 consumers inherit it: countInv14 (4162), findPairs (15563), verifyInvariants INV14 block (7375-7388, no extra gate). toggleSkip (9847) sets SKIPPED without touching SEL, so a skipped slot's retained SEL counts as a cook; an eat-out slot with no SEL counts via DEFAULTS. tryResolveCook refuses EAT_OUT/SKIPPED/MANUAL_SET slots (15596-15597), so the phantom pair persists and the resolver churns on the legit partner cook instead. INV14 is tracking-only → false tracking signal + reroll churn = MEDIUM.

### M8. [leftover-detector] `index.html:12850` — getRecentMealIds prev-week tail includes leftover (and skipped/eat-out) entries, contradicting documented leftover exclusion — over-blocks week-boundary picks

**Failure scenario**: Current-week reads exclude leftover-flagged (12828-9) per docstring; prev-week branch pushes every raw lastSel id. Last week X cooked Wed + leftovers Thu/Fri -> this Monday (b=3/4 reach prev Thu/Fri) X blocked though actual cook gap is 5 days (INV14-legal). Over-block only.

**Verdict (CONFIRMED)**: Current-week pushFromDay excludes leftover-flagged entries (12828-12829) per the docstring (12804-12805: 'Leftover entries are skipped'). The prev-week branch (12845-12859) pushes every raw lastSel id with no leftover filter (leftover slots carry the same SEL id, so they are present in lastSel). With dayIndex=0, lookback=4, b=3/4 reach prev-week Thu/Fri (12846: lastWeekDayIdx=7+prevIdx), so leftovers of a prev-Wed cook block the meal this Monday even though the cook gap is 5 days (INV14-legal — fires only at <5, 7380/15568). Deterministic over-block at every week boundary, contradicting the documented exclusion; recoverable via fallback cascade but wrong behavior = MEDIUM.

### M9. [leftover-detector] `index.html:12897` — isBatchLeftoverEligible accepts a SKIPPED/EAT_OUT prev-day slot as cook anchor — variety bypass with no real batch to extend

**Failure scenario**: Skipped/eat-out slot keeps SEL but computeLeftovers excludes it (4006), so los[prevKey] undefined and the lo.isLeftover guard passes -> returns true. Phase 1 (15797) admits the meal as batch continuation; no batch forms; fresh solo cook 2 days after phantom manufactures the INV14 pair above.

**Verdict (CONFIRMED)**: Traced: skipped/eat-out prev-day lunch/dinner retains SEL (toggleSkip 9847 never clears SEL), but computeLeftovers excludes the slot at 4006, so los[prevKey] is undefined. The only guard at 12899 is `if (lo && lo.isLeftover) continue` — undefined lo falls through to `return true` (12900). There is no SKIPPED/EAT_OUT check anywhere in isBatchLeftoverEligible (12877-12905). Phase 1 (15797) and all swap stages (e.g. 15615) then admit the meal as a batch continuation; no batch forms because the detector excluded the anchor slot. Combined with the ungated _collectHouseholdCooks (4104-4107), the fresh cook + skipped-slot SEL manufactures an INV14 tracking fire. Variety bypass with recoverable consequences = MEDIUM.

### M10. [pipeline-order] `index.html:15511` — V214 auto-skips are persisted and synced, but the transient tracker _autoSkipSlots is memory-only — after a reload the auto-skip becomes a permanent skip

**Failure scenario**: skipKcalOverSnacks commits SKIPPED[her_Thursday_snack]=true and records it only in the in-memory var _autoSkipSlots (declared line 2711, never persisted). randomizeWeek then calls autoSaveWeek → saveWeeks, which bundles SKIPPED into weekData.skipped and diff-stamps skippedTs (17127/17131), so the auto-skip is written to localStorage AND synced to the other phone as an ordinary skip. After the user closes/reopens the PWA (or on the receiving device), loadWeekState restores SKIPPED (17175) while _autoSkipSlots is {} — so the clear loop at 14554-14557 never removes it, and skipKcalOverSnacks itself early-returns on SKIPPED (15458). Every subsequent randomize keeps that snack skipped forever (no Set pill, user never chose it), directly contradicting the documented contract at 14549-14553 / 15409-15411 ('re-decided each randomize, NOT persisted... distinguishable from a user's manual skip').

**Verdict (CONFIRMED)**: SKIPPED[key]=true committed at 15504, tracked only in _autoSkipSlots (15511) — a bare var (2711) never written to weekData/localStorage/sessionStorage. saveWeeks persists+diff-stamps SKIPPED (17127, 17131) so it saves and syncs as an ordinary skip; loadWeekState restores it (17175). Post-reload _autoSkipSlots={} so the clear loop 14554-14557 is a no-op, skipKcalOverSnacks early-returns on SKIPPED (15458), and _randomizeWeekCore treats SKIPPED as user-only state it never clears (15714 comment, 15997). Contradicts the transience contract at 14549-14553/15409-15411. Recoverable (user can toggle the visible skip off) → MEDIUM.

### M11. [pipeline-order] `index.html:15628` — rerollInv14Violations mutates the NON-target person's cooks while gating misses and waste on the target person only

**Failure scenario**: randomizeWeek('him') (Her unchecked in the popup): him's winning SEL cooks chicken_stir_fry Wednesday lunch while her untouched week already cooks it Monday dinner → bestInv14>0 → rerollInv14Violations. persons=['him'] (15525), but findPairs uses _collectHouseholdCooks (15563), which returns BOTH persons' cooks, and tryResolveCook has no person filter — the V215 earlier-cook fallback (15677) swaps HER Monday dinner SEL at 15628 when the him-side cook can't be swapped. The accept gate (15643) measures totalMissCount over persons=['him'] only and totalWaste via _measuredTripWasteForPersons(['him']) → buildShoppingList(trip,'him'), which by the cook-anchor architecture EXCLUDES her-anchored batches — so a candidate that pushes her Monday fat% past 30% and adds her-side pkg waste passes all gates. Her plan is silently rewritten on a him-only randomize (contradicting the target contract at 13561-13563: 'only him's unlocked slots re-roll; her's stays'), with zero gating on her goal misses or her-side waste.

**Verdict (CONFIRMED)**: persons=[randTarget] (15525) but findPairs uses _collectHouseholdCooks (15563), which iterates both persons (4103), and tryResolveCook has no person filter — only EAT_OUT/SKIPPED/MANUAL_SET guards (15596-15597). Accept path commits SEL[sk(t.p,…)] for a her-side cook (15628, kept at 15643-15652) via later or earlier (15673/15677). Gates are him-scoped: totalWaste → _measuredTripWasteForPersons(['him']) → buildShoppingList(trip,'him') (15528-15529, 12642-12645; cook-anchor attribution excludes her-anchored batches per documented INV3-mirrored architecture), and dayMissCount iterates persons=['him'] (15533). Her plan silently mutated with her misses/waste unmeasured; hard-INV20 exposure on him-only runs is already covered by the 14548 finding, so MEDIUM stands.

### M12. [render-display] `index.html:640` — fmtFrac dropdown-label snap band is 0.0601 while the documented round-trip contract is 0.02 — display can misrepresent stored amount 3x tolerance; two indistinguishable options possible

**Failure scenario**: 0.29 cup shows as "quarter" glyph (val .29) adjacent to a real 0.25 "quarter" option; 5.55 tbsp shows 5.5 (err .05). Related INV25 tautology filed under inv-checker (6996).

**Verdict (CONFIRMED)**: fmtFrac snap band is 0.0601 at both sites (640, 646). buildQtyOptions inserts the off-grid stored amount with label fmtFrac(currentAmt) when no option is within 0.02 (768-778). Stored 0.29 cup: not found (0.25 dist 0.04), pushed as val 0.29 labeled '¼' (dist 0.04 ≤ 0.0601) adjacent to the real val-0.25 '¼' option — two indistinguishable options. tbsp 5.55: labeled '5½' next to real 5.5 (err 0.05). Documented INV25 contract (CLAUDE.md Card Display) is 0.02 round-trip; label-side error up to ~0.06 is 3x that. Doc-vs-code contradiction, code side wrong. MEDIUM: display misrepresentation plus indistinguishable-option mis-selection risk.

### M13. [render-display] `index.html:8381` — Shared-card feeds-days badges collect ALL leftovers with matching cookDay — no batch/meal filter; unrelated batches and sameDayShared portions produce wrong day badges

**Failure scenario**: Badge scans entire leftovers map for isLeftover && cookDay===day, ignoring anchorLoSV.portions in scope 5 lines earlier. Mon lunch batch A (Wed leftover) + Mon shared dinner B (Thu leftover) -> B's card badges Mon+Wed+Thu: Wed is phantom (meal A), Mon is the sameDayShared portion the contract says is not a leftover.

**Verdict (CONFIRMED)**: Lines 8378-8386 scan the ENTIRE leftovers map for lo.isLeftover && lo.cookDay===day — no meal-id, slot, or portions filter, and no !lo.sameDayShared exclusion (contrast the himLo check at 8373). anchorLoSV.portions is in scope at 8369-8371 and unused. So on a shared dinner B's card (batchSize>2 gate at 8376), a Monday-cooked lunch batch A's Wednesday leftover badges Wed (phantom), and B's own sameDayShared portion (isLeftover:true, cookDay=day per detector contract) badges the cook day itself, contradicting the sameDayShared not-a-leftover display rule. MEDIUM: wrong batch-structure info displayed; recoverable.

### M14. [render-display] `index.html:8747` — _wasteHint splits trip totals by portion-day while shopping/freeze/INV20 attribute full batches to the anchor trip — spurious waste hints on cross-trip batches

**Failure scenario**: sumDays (8685-97) counts portions on eaten day; buildShoppingList (4727-42), _freezeOneItem (11550-92), INV20 attribute to anchor trip. Wed-cook + Thu-leftover broccoli batch frozen clean at 2.0c=1 head -> manual edit banner claims 0.85c waste + suggests ±; following it creates real waste. Carry branch (8717-8) same divergence.

**Verdict (CONFIRMED)**: sumDays (8685-8697) iterates every person/day/slot in the trip's days including leftover slots, splitting a batch's amounts by portion-eaten day. Shopping (4720-4742) skips leftover slots and attributes the full batch to the anchor's trip when the cook day is in-trip (4722, 4725, 4731-34); _freezeOneItem (11556-11557, 11565-11584) likewise sums full batch totals from anchors only. Wed-cook/Thu-leftover 2.0c batch frozen to 1 head: sumDays gives 1.0 per trip → cook1 pkgs=ceil(1.0/2.0)=1, waste=1.0 ≥ 0.05 (8750-8752) → spurious hint. Carry branch uses the same sumDays (8717-8718). MEDIUM: misleading hint; following its ± suggestion creates real waste.

### M15. [shopping-sync] `index.html:4862` — priorCarry depleted per shopping view: Him-only/Her-only views each consume the full household carry -> single-person views under-buy or lose lines entirely

**Failure scenario**: carryDemand accumulated only for persons in view (4799) but full household priorCarry applied in every view. Her-only view: demand.him.cook1=0 -> entire carry depletes her demand -> her broth/marinara line vanishes though him's cooking consumed the open jar. Both single views under-buy vs Both view. INV3 validates only who=both (6448).

**Verdict (CONFIRMED)**: persons filtered by who (4628); carryDemand accumulated only via persons.forEach (4799), so Her-only view has demand.him.cook1=0. Full household priorCarry (4852-4853) then depletes him first (take=min(carry,0)=0, 4864-4868) and falls entirely on her (4869-4873), shrinking effC1.her below the both-view value; line vanishes when displayedAmt<=0.001 (4891). Cook2 view also diverges via the c1<0.001 branch (4882). INV3 validates only buildShoppingList(trip,'both') (6448). Data-dependent: needs crossTripCarry item + priorCarry + her-anchored c1 demand. Both view (the INV3-validated canonical list) stays correct, so recoverable — MEDIUM.

### M16. [shopping-sync] `index.html:17367` — Merge iterates only SEL keys — ingredient-edit overrides on DEFAULTS-fallback slots (no SEL entry) never sync; Force Push no-op for them too

**Failure scenario**: allKeys = keys(remoteSel) ∪ keys(localSel); _writeUserPin (10584) bumps ts[k] but never creates SEL[k]. Fresh next-week (sel:{}) edit on a DEFAULTS-rendered card -> override/manualSet/userEdited never enter allKeys, silently never transfer. R12-M2 comment at 10577 claims propagation.

**Verdict (CONFIRMED)**: allKeys built solely from remoteSel/localSel keys (17366-17368); overrides/manualSet/userEdited apply only inside that loop (17394-17396). _writeUserPin writes OVERRIDES via setOverride (3708-3713), sets MANUAL_SET/USER_EDITED, and bumps ts via _bumpSlotTs (10584 → 5061-5064) — but never creates SEL[k]; the solo entry path (10696) has no stampSel. getMealId falls back to DEFAULTS when SEL absent (3962), so editing a DEFAULTS-rendered card (e.g. fresh next week, sel:{} per 17353) produces an override with ts but no sel key on either device → never enters allKeys, never syncs. Force Push bumps ts only for sel keys (17717), and the receiver loop still can't apply it. R12-M2 comment (10577-10583) claims propagation — wrong for this case.

### M17. [shopping-sync] `index.html:17472` — LOCK_MANUAL_SLOTS guards only the SEL-bundle merge; independent skipped/eatOut (and lateSnack) per-key merges overwrite locked manualSet slots

**Failure scenario**: Lock check exists solely at 17388. V51 skipped merge (17472), eatOut merge (17488), lateSnack merge (17406) apply remote rt>lt writes to locked slots. Protected slot gets remotely skipped/unskipped, contradicting documented immunity + UI promise at 17862.

**Verdict (CONFIRMED)**: LOCK_MANUAL_SLOTS check exists only inside the SEL-bundle loop (17388). The independent V51 skipped merge (17472-17480), eatOut merge (17488-17496), sharedSchedule merge (17429-17444), and lateSnack merge (17406-17414) apply any remote rt>lt write with no manualSet/lock test. The UI promise (17853-17862) explicitly says slots where you 'toggled shared/skip/eat-out' won't be overwritten by incoming sync. So a locked slot's skip/eat-out/shared state is remotely overwritten despite the documented immunity. (lateSnack keys are pk(p,d) so slot-keyed manualSet couldn't guard them as-written — same gap.)

### M18. [shopping-sync] `index.html:17554` — calBase/proTarget LWW migration hole (pre-R13 customization has ts=0, still blindly clobbered) + >= tie is remote-wins unlike every other key *(upgraded from finder severity LOW)*

**Failure scenario**: CAL_BASE_TS stamped only by R13 setter (7847), initializes 0 (5395). Pre-R13-customized value + pre-R13 partner payload (no calBaseTs) -> 0>=0 remote default overwrites local custom. Tie rule >= vs strict > elsewhere.

**Verdict (CONFIRMED)**: CAL_BASE_TS initializes 0 (5395) and is stamped only by the R13 setter (7847; proTarget analog nearby), so any pre-R13 customization sits at ts=0. Merge condition `(remote.calBaseTs||0) >= CAL_BASE_TS` (17554/17555): payload always carries calBase+calBaseTs (17305-17307), so a partner who never customized post-R13 sends ts=0 and 0>=0 clobbers the local custom value with the remote default — on every pull, until the user re-sets targets. The `>=` tie is remote-wins, unlike strict `rt>lt` at every other merge site (17389, 17409, 17432, 17475, 17491). The R13 comment (17550-17553) justifies only the fresh-install tie, not the pre-R13-customized case. Silent recoverable loss of a user setting → MEDIUM, data-dependent (migration window).

### M19. [shopping-sync] `index.html:17717` — Force Push bumps timestamps only for keys PRESENT in local maps — locally-absent keys survive on the other phone; phones stay divergent

**Failure scenario**: Bump loops iterate local value maps (sel, skipped, eatOut, lateSnack, sharedSchedule, adjTargets). A skip/SEL pick/late snack that only phone B has keeps its ts; B pulls, rt=0<lt -> B keeps it, defeating the operation's stated purpose (comment 17701-13 "nothing on remote can win").

**Verdict (CONFIRMED)**: Force Push bump loops iterate only LOCAL key sets: sel (17717), lateSnack (17719), sharedSchedule (17721), skipped (17728), eatOut (17730), adjTargets (17739). A key existing only on phone B keeps B's local ts; after B pulls A's force-pushed payload, rt=(remote ts map)[k]||0=0 < lt, so B keeps its entry at every merge site (17389, 17409, 17475, 17491, 17536). The bundled-state delete branches (17392-17396) run only when remoteTime>localTime, never for B-only keys. Contradicts the stated semantics 'nothing on remote can win' / 'replace its state with yours' (17701-17704, 17710). Phones remain divergent — recoverable wrong behavior.

### M20. [ui-state-overrides] `index.html:9850` — toggleSkip OFF (and toggleEatOut OFF / _v70 / _v85) delete MANUAL_SET but leave _userSet OVERRIDES — orphaned pins: slot unlocked for randomize yet still shows edit banner and Reset button

**Failure scenario**: User pins an ingredient amount on him Tue dinner (_writeUserPin sets OVERRIDES._userSet + MANUAL_SET). User taps Skip ON (MANUAL_SET stays) then Skip OFF: lines 9848-9854 delete SKIPPED, MANUAL_SET, and USER_EDITED but leave the _userSet override untouched (same asymmetry in toggleEatOut OFF at 10017, _v70UndoEatOutSide 3356, _v85ClearSideVisualIfAny 3453-3456). Resulting state: _hasUserOverrides is still true so the card renders the '✏️ Manual edits' banner plus ↺/⚖ buttons (9192-9193), but the Set pill is gone and isLockedForRandomize is false. At the next randomize the slot is treated as free: Phase 1's clearOverrides (15929) silently discards the user's edit when it re-picks the slot — or, on a shared cell, the pin survives into the her-mirror path (finding 1) and lands on a different meal. Every other unlock path (onIngrReset) clears the user overrides together with MANUAL_SET; the skip/eat-out OFF paths do not.

**Verdict (CONFIRMED)**: Asymmetry verified at all four cited sites: toggleSkip OFF deletes SKIPPED/MANUAL_SET/USER_EDITED only (9848-9854); toggleEatOut OFF (10015-10030), _v70UndoEatOutSide (3350-3357), and _v85's skip/manual-purple branches (3441-3448, 3453-3456) likewise never touch OVERRIDES. _hasUserOverrides checks _userSet on OVERRIDES (3721-3727) so the edit banner plus reset/rebalance buttons still render (9192-9193) while isLockedForRandomize (2748) is false. Next randomize: Phase 1 clearOverrides (15929) silently discards the edit on unlocked slots, or the pin survives into the shared-sync path (line 15956 finding). onIngrReset by contrast clears overrides with MANUAL_SET (10797-10806, 10840). MEDIUM: inconsistent recoverable state, plus producer for the CRITICAL finding.

### M21. [ui-state-overrides] `index.html:10429` — confirmTempIngredient computes nextIdx from the tapped slot's overrides only — batch-wide write merges into and destroys an existing appended user ingredient on other portions

**Failure scenario**: him Mon dinner (solo, recipe length 8) gets a temp ingredient via '+' → override idx 8 (_userSet) on Mon only; MANUAL_SET set. A later randomize extends it into a batch (Phase 1's isBatchLeftoverEligible allows the same meal on Wed dinner, cook day+2 window) — Wed has no overrides. User now taps '+' on the WED card: nextIdx is computed only from OVERRIDES[him_Wednesday_dinner] (empty) → nextIdx=8 (10427-10429). The batch loop (10439-10454) then calls setOverride(him,Monday,dinner,8,...) — setOverride (3711-3713) finds Mon's EXISTING idx-8 entry and merges field-by-field, overwriting its dbKey/amt/_priorAmt with the new ingredient's values. The first user-added ingredient is silently destroyed: after the next cache rebuild (any invalidateLeftoverCache), applyOverrides' appendix (3936) emits only the new idx-8 ingredient, so cards and the shopping list lose the original add on every portion with no warning. nextIdx must be the max across ALL batch portions' overrides, not just the tapped slot's.

**Verdict (CONFIRMED)**: confirmTempIngredient computes nextIdx from the tapped slot's OVERRIDES only (10426-10429). _getBatchPortions (10603-10617) walks a leftover to the anchor's full portion list, so the batch loop (10439-10454) writes idx=nextIdx to the anchor slot too. setOverride merges by idx into an existing entry field-by-field (3711-3713), overwriting the anchor's earlier appended ingredient's dbKey/amt/_priorAmt. After any rebuild, applyOverrides' appendix (3936-3948) emits one ingredient per idx>=recipeLen, so the original add disappears from cards and shopping on every portion. Batch formation around a MANUAL_SET anchor is reachable (Phase 1 isBatchLeftoverEligible, or manual assignment). Silent single-ingredient overwrite, recoverable — MEDIUM.

### M22. [unify-freeze-waste] `index.html:12670` — _measuredTripWasteForPersons produce branch counts softWaste fires as hard, diverging from the INV20 verifier it mirrors (pkg branch subtracts soft, 12660)

**Failure scenario**: banana/avocado/orange/jalapeno count hard here while verifyInvariants hard = INV20-INV20Soft = 0. Consequences: rerollMissDays guard (14965) rejects goal-miss-fixing swaps to protect accepted-by-design soft waste; retry early-exit totalWaste===0 (14700) unreachable in any week with a soft produce fire -> all 60 retries always run; lexicographic selector ranks soft waste above INV14 contrary to documented criteria. Also missing the crossTripCarry per-trip skip (7580/7621).

**Verdict (CONFIRMED)**: Produce branch increments total unconditionally on threshold (12665-12673) while pkg branch subtracts soft via !db.pkg.softWaste (12660). The verifier it claims to match 'exactly' (comment 12664) emits INV20Soft for softWaste produce (7633-7635) and hard = INV20-INV20Soft. banana/avocado/orange/jalapeño (softWaste produce) count hard here. Consequences verified: retry early-exit requires totalWaste===0 (14700); lexicographic selector ranks it above inv14 (14689-14692); rerollMissDays guard newWaste<=baseWaste (14965) via totalWaste()=_measuredTripWasteForPersons (14857-14858). Also both branches lack the verifier's crossTripCarry skips (7580, 7621) — pkg side currently masked by softWaste.

### M23. [unify-freeze-waste] `index.html:14799` — Stage 2 carry-absorb hardcodes persons=[him,her] — a him-only randomize can silently swap HER meals

**Failure scenario**: Every other post-retry stage scopes to randTarget (14853, 14648, 10997). Absorb passes both persons unconditionally; _absorbFindCandidates (12306) enumerates her slots; commit changes her SEL + deletes her OVERRIDES during a him randomize; post-commit re-pin freezeTripTotals("him",false) leaves her swapped slot unfrozen relative to the jar target.

**Verdict (CONFIRMED)**: Call site hardcodes ['him','her'] (14799) while sibling stages scope to randTarget (14648, 14853, 10997). randTarget='him'/'her' is reachable: Randomize popup sets it (13250-13255) and edit-mode apply uses the current person tab (13564-13565). _absorbFindCandidates enumerates the passed persons' non-MANUAL_SET slots (12302-12311); commits mutate SEL and delete OVERRIDES (12410-12413) with no revert on success. Post-commit re-pin freezeTripTotals(randTarget,false) (14812) builds persons=[randTarget] (10997), so her swapped slot's carry/pkg amounts are never re-frozen during a him-only randomize. _absorbHardINVsClean (12419) gates commit-time hard fires, so recoverable — MEDIUM.


## LOW (14)

### L1. [adjuster-balancer] `index.html:1003` — Skip-if-close threshold is Math.min(15%, 80 kcal) — contradicts CLAUDE.md and its own comment ('within 15% OR 80 kcal')

**Failure scenario**: CLAUDE.md Calorie Adjuster step 1 ('If recipe is within 15% or 80 kcal of budget, no adjustment') and the inline comment at line 996 ('15% of base kcal OR 80 kcal') describe OR semantics: skip when delta < max(15% x base, 80). The code uses Math.min, i.e. skip only when delta is within BOTH bounds. Concrete: 400-kcal-base snack with targetDelta = +70: doc says within 80 -> leave unchanged; code computes threshold = min(60, 80) = 60 -> 70 >= 60 -> the meal IS scaled. For every meal under ~533 base kcal the documented 80-kcal skip band never applies. Behavior may be intentional (tighter is safer) but doc and comment both misstate what ships.

**Verdict (CONFIRMED)**: Line 1003: threshold = Math.min(baseKcal*0.15, 80) — skip fires only when |targetDelta| is within BOTH bounds (AND semantics; effective band = the smaller). The inline comment at 996-997 ('15% of base kcal OR 80 kcal') and CLAUDE.md Calorie Adjuster step 1 ('within 15% or 80 kcal') both describe OR semantics (= max). Traced example holds: baseKcal 400, delta 70 → threshold min(60,80)=60 → 70 ≥ 60 → meal IS scaled, contradicting the documented 80-kcal skip band; for any base < ~533 kcal the 80-kcal bound never applies. Doc-vs-code contradiction: the code ships tighter behavior; comment and CLAUDE.md misstate it. LOW (doc/comment defect; behavior may be intentional).

### L2. [adjuster-balancer] `index.html:5856` — balanceDayMacros dailyFV() omits LATE_SNACK veg/fruit that computeDailyFV (V144) counts — P4 boosts veg on days already at target

**Failure scenario**: V144 added late_snack ingredient FV to computeDailyFV/computeSlotFV (line 8099+), but the balancer's private dailyFV() (lines 5856-5871) still sums only the slots map, ignoring LATE_SNACK[pk(p,d)].ingredients — while its kcal twin dailyMacros() DOES include the late snack. Concrete: her day with 2.5c veg across meals + 0.5c baby spinach in a late snack: computeDailyFV = 3.0c (daily veg goal met) but the balancer computes fv.veg = 2.5 -> vegGap = 0.25 -> P4 boosts a meal's veg 0.25c/pass (each pass re-fires while the phantom gap persists, until the 3x-base/maxAmt caps stop it), adding unneeded veg kcal to a day that already met the target. Benign direction (extra veg) but a live divergence between the two FV views that V144 was supposed to close.

**Verdict (CONFIRMED)**: dailyFV (5856-5871) iterates only Object.keys(slots) — no LATE_SNACK term — while its kcal twin dailyMacros includes LATE_SNACK[pk(p,d)] at 5826-5827, and computeDailyFV includes ls.ingredients via computeSlotFV at 8105-8109 (V144 comment at 8099). So a late-snack veg counted by computeDailyFV is invisible to the balancer: vegGap = 2.75 - fv.veg (6066) stays positive and P4 (6279-6282) boosts meal veg on a day already at target, re-firing each pass until 3×-base/maxAmt caps in boostFV (6006-6009) stop it. Benign direction (extra veg, small kcal), but a live V144 divergence. LOW stands.

### L3. [inv-checker-and-db] `index.html:19` — INVARIANTS.md documents only INV1-16 — INV17-26 missing from the file CLAUDE.md cites as the full rule reference

**Failure scenario**: Code implements INV17-26 (7409-7795, 19145-75); the designated quick-reference has no contract for 10 invariants incl. hard INV17/19/20/23-26.

**Verdict (CONFIRMED)**: INVARIANTS.md ends at line 18 with the INV16 row — only INV1–16 are documented. Code implements and emits INV17–26 (e.g. INV20/INV20Soft/INV21/22 at 7775-7782, INV20Leftover at 7792, INV23 at 6893, INV24 at 6919-6944, INV25 at 7000; aggregator keys through INV25 at 18901), and CLAUDE.md's table lists INV1–26 while citing INVARIANTS.md as the 'full detailed rules' reference. Ten invariants including hard INV17/19/20/23–26 have no contract in the designated quick-reference. Doc incompleteness; LOW.

### L4. [inv-checker-and-db] `index.html:7132` — INV8 isClean accepts an undocumented thirds grid at looser 0.003 tolerance — docs stale on the actual acceptance set

**Failure scenario**: CLAUDE.md + INVARIANTS.md say <=2 decimals OR 1/8 fractions at 0.001; code additionally passes 1/3-grid values at 0.003 (deliberate per comment, fmtFrac renders third-glyphs). Docs wrong side.

**Verdict (CONFIRMED)**: isClean (7125-7134) accepts a thirds grid at 7132 (|v*3 − round(v*3)| < 0.003, i.e. 0.001 in value space — the finder's 'looser 0.003' is only looser in ×3 space), deliberate per the 7127-7129 comment (fmtFrac renders ⅓/⅔ glyphs). Exact 1/3 (0.3333…) is neither 2-decimal (off by 0.0033 > 0.001) nor 1/8-grid, so the acceptance set is genuinely wider than documented. CLAUDE.md INV8 row and INVARIANTS.md line 10 both state only '≤2 decimals OR 1/8 fractions', tolerance 0.001 — docs are the wrong side. Doc-vs-code contradiction; LOW.

### L5. [inv-checker-and-db] `index.html:15900` — Doc-vs-code: DISCOURAGED_INGREDIENTS penalty is 3000/meal in code+comment+memory; CLAUDE.md Data Layer says 500

**Failure scenario**: CLAUDE.md wrong side; 6x mis-model for anyone tuning selection pressure.

**Verdict (CONFIRMED)**: Code at 15900: discouragedPenalty = discouragedCount * 3000, with the comment at 15889-15893 explicitly saying '3000/meal dominates typical calDiff (0-500)'. MEMORY.md conventions also say 3000pt. CLAUDE.md Data Layer (DISCOURAGED_INGREDIENTS entry) says '500-point penalty per meal' — CLAUDE.md is the wrong side, a 6× mis-model of selection pressure. Doc-only defect; LOW.

### L6. [leftover-detector] `index.html:3983` — Stale detector header-comment describes a "Shared co-anchor {isLeftover:false, shared:true, sharedWith}" shape the code no longer emits

**Failure scenario**: Code emits {isLeftover:true, sameDayShared:true} (4076-82); sharedWith never assigned anywhere; second stale ref at 14023. In-file comment is wrong side; CLAUDE.md correct.

**Verdict (CONFIRMED)**: Header comment (3982-3984) documents a 'Shared co-anchor {isLeftover:false, shared:true, sharedWith}' emission. The code emits the same-day other-person portion as {isLeftover:true, ..., sameDayShared:sameDay&&e.p!==anchor.p} at 4076-4082; grep confirms `sharedWith` appears only at 3984 and in the stale unifyCrossPersonRatios comment at 14023 ('sharedWith entries are skipped'), never assigned. The in-file comments are the wrong side; CLAUDE.md's description (one anchor, sameDayShared leftover) matches the code. Stale comments only, no behavior impact = LOW.

### L7. [leftover-detector] `index.html:4027` — Doc-vs-code: CLAUDE.md says breakfast/snack can anchor a batch if user manually sets; cookSlots is unconditionally [lunch,dinner]

**Failure scenario**: No manual-set branch exists (3991). Doc likely wrong side (conflates detector with manual leftoverLinks paint groups).

**Verdict (CONFIRMED)**: cookSlots is unconditionally ['lunch','dinner'] (3991) and the anchor gate at 4027 rejects any other slot; computeLeftovers (3986-4091) contains no MANUAL_SET or leftoverLinks branch that would let a manually-set breakfast/snack anchor a batch. CLAUDE.md's 'breakfast/snack can be in a batch only as the anchor if user manually sets' describes a nonexistent detector path — the doc is the wrong side (likely conflating the detector with manual leftover-link paint groups, which are a separate visual mechanism). Doc-vs-code contradiction, no runtime misbehavior = LOW.

### L8. [leftover-detector] `index.html:12837` — Phase 1 recentIds reads stale not-yet-committed SEL (forward window + today), contradicting the actively-picked-this-randomize docstring

**Failure scenario**: Forward/today reads during Phase 1 day-by-day commit see the previous retry (or incumbent plan) picks for not-yet-committed days -> over-blocking search bias; fallback cascade + 60 retries recover; no invariant reachable.

**Verdict (CONFIRMED)**: Phase 1 computes recentIds once per person/day (15729) and commits SEL per day only at 15926-15937; _randomizeWeekCore's pre-phase (15702-15717) never wipes SEL, and the retry loop reruns the core on mutated SEL. So the today window (12836-12838) and forward window (12863-12869) read SEL for slots/days not yet committed by the current retry — i.e. the previous retry's (or incumbent plan's) picks — contradicting the docstring's 'what's been actively picked this randomize' (12816-12818). Effect is only additive to `recent` → over-blocking search bias; incomplete-attempt guard (15834-15836, 15906) plus 60 retries recover, no invariant reachable = LOW.

### L9. [pipeline-order] `index.html:14643` — CLAUDE.md retry-loop description is stale: pipeline runs on EVERY retry, not only when a retry hits totalWaste===0

**Failure scenario**: CLAUDE.md ('Randomize Pipeline' → Retry loop) states 'When a retry hits totalWaste===0, it runs the FULL post-balance pipeline (RBA + freeze) inside the measurement' — but since V97 the code at 14643-14645 runs runBalanceAdjusters + freezeTripTotals + runBalanceAdjusters unconditionally on all 60 retries (comment 14640: 'we COMMIT to running the pipeline (no conditional gate)'). The doc's post-retry step list (1-11) also omits _autoApplyFatBoosts (14734), which runs between rerollInv14Violations and the first balancer pass and writes overrides. A future session trusting the doc would mis-model per-click cost (~50ms × 60 retries) and miss a state-mutating pipeline stage when reasoning about override lifecycles.

**Verdict (CONFIRMED)**: Doc-vs-code contradiction; code side is right. runBalanceAdjusters+freezeTripTotals+runBalanceAdjusters run unconditionally on every retry (14643-14645), with the comment at 14640-14642 explicitly stating 'we COMMIT to running the pipeline (no conditional gate)' and the cost note at 14636-14637 ('pipeline runs on every retry ~50ms each'). CLAUDE.md's 'When a retry hits totalWaste===0, it runs the FULL post-balance pipeline' is stale. The doc's post-retry step list (1-11) also omits _autoApplyFatBoosts, which runs at 14734 between rerollInv14Violations (14727) and the first balancer pass (14737) and writes overrides (3888-3892). Doc-only defect → LOW.

### L10. [render-display] `index.html:8992` — leftoverBadge (cook-day label incl. his/her attribution) is built but never rendered — documented "Leftovers <day>" pill shows no day

**Failure scenario**: Assigned at 8988/8992, never concatenated into returned HTML (9178-98); only leftoverPill ("Leftovers") renders. Info exists only on Shared-view badge (8374-5). Likely code wrong side (badge meant to render).

**Verdict (CONFIRMED)**: Grep: leftoverBadge appears only at 8988 (init) and 8992 (assignment) in the whole file. It is a local of buildMealCardHtml and the returned HTML (9154-9158 topRowPills, 9178-9198) concatenates only leftoverPill/cookPill/etc. — leftoverBadge is never emitted. The rendered pill (8993) is plain 'Leftovers', while CLAUDE.md Card Display documents a 'Leftovers <day>' pill; the his/her+cookDay attribution (8991) exists but is dead. Day info only reaches the Shared-view badge (8374-8375). LOW: dead code plus doc-vs-code mismatch (code side missing the documented day label); no amount or state impact.

### L11. [shopping-sync] `index.html:4996` — Doc-vs-code: CLAUDE.md claims shopping waste warnings skipped for acceptableWaste/nonWaste; pkg branch deliberately skips only longShelfLife (V117)

**Failure scenario**: acceptableWaste/nonWaste skip exists only in the PRODUCE branch (5022); coconut milk (pkg, acceptableWaste) still shows a warning by design (comment 5015-17). Doc side overbroad.

**Verdict (CONFIRMED)**: Doc-vs-code confirmed; doc side is wrong. The pkg waste-warning branch gates only `!db.pkg.longShelfLife` (4996) — no acceptableWaste/nonWaste check — and the V117 comment (5013-5017) states this is deliberate: pkg keeps its warning even for acceptableWaste items (coconut milk). Only the produce branch gates `!acceptableWaste && !nonWaste` (5022). CLAUDE.md's 'Waste display in shopping list … Skipped for longShelfLife / acceptableWaste / nonWaste' is overbroad for pkg items. Code behavior is intentional; fix belongs in the doc. LOW.

### L12. [ui-state-overrides] `index.html:10522` — _writeUserPin V176/V177 recipe fallbacks index the UNfiltered meal.ingredients while cache/display idx space is the amt>0-filtered meal[p].ingredients

**Failure scenario**: expandMealIngredients (677-679) builds meal[p].ingredients by filtering out amt<=0 entries — her_shake has I('peanut butter',0,'fat') at meal.ingredients[4], so meal.her.ingredients[4] is 'oats tbsp' (indices shifted by one from idx 4 on). The cache, dropdowns and override idx all use the filtered per-person list, but _writeUserPin's cache-empty fallbacks read _meal.ingredients[idx] directly: the swap-detection at 10522-10524 and the _priorDbKey capture at 10556-10560. If the user swaps/edits her shake's row 4 while _dayBalancedCache is empty (the exact window V176 was added for — after invalidateLeftoverCache, before the next rebuild), the fallback reads 'peanut butter' instead of 'oats tbsp': _priorDbKey is captured wrong, so the manual-edit banner (8807, 8839) shows the wrong prior ingredient and computes kcalDiff against peanut butter's per-tbsp kcal instead of oats'. Contrast with the sibling fallback at 10247 which correctly uses oldMeal[p].ingredients.

**Verdict (CONFIRMED)**: expandMealIngredients filters amt<=0 when building meal[p].ingredients (677-682), so per-person/cache/override idx space is shifted for any recipe with a zero-amt entry — her_shake has I('peanut butter',0,'fat') at raw index 4 (1383) with 'oats tbsp' at filtered index 4. _writeUserPin's swap-detection (10522-10524) and _priorDbKey fallback (10556-10560) read unfiltered _meal.ingredients[idx]; the sibling in _captureUserPinsForMealChange correctly uses oldMeal[p].ingredients (10247). Impact is bounded: the lazy getBalancedSlotIngredients fallback (10545-10550) usually supplies the correct priorDbKey first, so the wrong read affects swap-detection (refreshPrior) and the last-resort priorDbKey — banner label/kcalDiff only. Data-dependent (zero-amt recipes exist built-in and via forms). LOW cosmetic.

### L13. [unify-freeze-waste] `index.html:7720` — Weekly INV20 carry site does not gate pkg.longShelfLife (per-trip site gates all three flags) — CLAUDE.md claims symmetry the code lacks

**Failure scenario**: A {longShelfLife,crossTripCarry} pkg item would skip per-trip yet hard-fire weekly when priorCarry exceeds c1 demand. Same defensive-gap class V219 fixed for acceptableWaste/nonWaste. Code side missing the guard.

**Verdict (CONFIRMED)**: Weekly carry site gates only acceptableWaste/nonWaste (7720-7721, V219); per-trip site also gates longShelfLife (7580). CLAUDE.md INV20 row claims both sites gate longShelfLife/acceptableWaste/nonWaste — code lacks it at the weekly site. A {longShelfLife, crossTripCarry} pkg item skips per-trip (7580) but reaches the weekly fire (7763-7777): priorCarry>houseC1 makes actualWaste (7748) exceed the 1% pct check → hard INV20 despite longShelfLife meaning 'excluded from waste analysis'. No current DB item carries both flags (not a valid refutation per project rules) — data-dependent defensive gap plus doc-vs-code mismatch, code side missing the guard. LOW stands.

### L14. [unify-freeze-waste] `index.html:14240` — Doc-vs-code: unify fast-path skip documented as 0.5%-of-target; code uses 0.001 absolute (tightened 2026-04-27 per in-code changelog)

**Failure scenario**: Doc reasoning up to ~5x looser than reality on typical amounts. Doc stale; code authoritative.

**Verdict (CONFIRMED)**: Executable fast-path tolerance is 0.001 absolute: Math.abs(amt-final)>0.001 forces write (14239-14240), with in-code changelog documenting the 2026-04-27 tightening from 0.5% relative (14233-14238). CLAUDE.md's Cross-Person Unification section still says 'Fast-path skips a portion within 0.5% of target' (and even the older block comment at 14215-14218 repeats 0.5%). The force-write on minAmt/maxAmt violation does match doc (14226-14229). Doc side is stale; code authoritative. Pure documentation defect — LOW.
