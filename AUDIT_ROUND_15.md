# Audit Round 15 — 2026-07-13 (V274 @ `main` 9a56588)

> **Fix status (updated as waves land):**
> - **2026-07-18 V275**: all 3 CRITICALs FIXED (SOLO-1, SOLO-2, c1) — see the ✅ notes on each entry.
> - **2026-07-18 V277**: the 8 V253 menu-op parity MEDIUMs FIXED (v253-shared-on-unconditional-v70, v253-eo-on-autoshared-clears-other-side, v253-clear-rerolls-default-and-autoshared-cells, v253-state-checks-dont-mirror-grid, c2, c3, c4, c5) — see the ✅ notes on each entry.
> - **2026-07-18 V278**: pantry netting FIXED (R15-PANTRY-1, R15-PANTRY-2, + the R15-PANTRY-3 LOW) — user picked the sequential-depletion model. See the ✅ notes on each entry.
> - **2026-07-18 V279**: the remaining 10 MEDIUMs FIXED (R15-SETTINGS-1, R15-SHELF-1, R15-SYNC-1, m1, m2, m3, SOLO-3, SOLO-4, SOLO-5, R15-CARRY-1) — **all 3 CRITICALs and all 20 MEDIUMs are now closed**; only the LOW sweep remains. See the ✅ notes on each entry.

**Scope**: everything shipped since Round 14 closed at V239 — V240–V274: INV27 + selfTestInvariants,
Shelf/settings/solo mode (V242/V244), pantry subtraction (V244), meal move/swap (V243), the card/theme
redesign (V245–V252), and the schedule-grid tap-for-menu redesign + visual iterations (V253–V274) —
plus standing cross-checks (pipeline drift, invariant coverage, docs).

**Method**: chunked multi-agent adversarial audit (session-limit-resilient per user direction):
- Chunk 1 (3 finder agents): V253 menu-op parity, paint arming, V243 move/swap → 30 findings.
  The 24-agent verify wave died at a session limit; the 6 highest-stakes findings were instead
  verified by the PRIMARY agent via direct code reads (marked CONFIRMED below); the rest carry
  finder evidence and are marked PLAUSIBLE pending fix-time re-checks.
- Chunk 2 (2 finder agents): solo-mode completeness, pantry/settings/sync → 22 findings;
  both CRITICALs verified by primary-agent code reads.
- Chunk 3 (primary agent self-audit after finder agents hit a usage limit): cards CSS + grid render
  → dead-code inventory + clean confirmations (escaping, ordering, harness couplings).
- Chunk 4 (mechanical): git-diff pipeline drift check + docs spot-check.
- Chunk 5 (runtime gates, fresh pane, pristine protocol): selfTestInvariants + Tests 1 & 2.

**Totals after dedupe: 49 findings — 3 CRITICAL · 20 MEDIUM · 26 LOW** (+7 self-audit LOWs and one
informational clean-check record in the chunk 3/4 section). Dedupes: SOLO-6≡R15-CARRY-1 (merged),
c7≡R15-DEAD-5 (merged), v253-solo-mode-disabled-person-mutation folded into SOLO-3.

**Runtime gates (all green — the pipeline itself is untouched and healthy)**:
- `git diff V239..V274`: **zero modified pipeline function definitions** (adjusters, unify, snaps,
  RBA, freeze/absorb, detector, balancer, randomize core, rerolls, shopping, calcTotals,
  setSchedule/applyScheduleEdit/paintAccept). Only new functions + render/UI/sync/harness hunks.
- `MPStress.selfTestInvariants()`: 18/18 pass, documented skips only.
- Test 1: primary 99.64%, hard INVs all 0, INV14 0, INV20 17 all-soft — byte-identical to baseline.
- Test 2: primary 99.64%, hard INVs all 0, INV14 0, INV20 14 all-soft.

**The headline pattern**: every CRITICAL and most MEDIUMs live at FEATURE INTERSECTIONS the stress
harness cannot see — solo mode × edit-mode Reset, solo × link-group paint, menu ops × the old tap
handlers' accumulated guards, pantry units × display units. The randomizer/invariant machine is clean;
the new UI state machinery around it is where the debt landed.

## The three CRITICALs (all primary-agent CONFIRMED — all FIXED in V275, 2026-07-18)

1. **SOLO-1 — edit-mode Reset permanently destroys the disabled person's plan.**
   `clearScheduleEdit` wipes both persons' SEL/LATE_SNACK/flags unscoped; Apply's randomize is
   solo-coerced and repopulates only the enabled person; the disabled person's emptiness is invisible
   (their rows don't render, getMealId returns null) and unrecoverable after Apply discards the
   snapshot; autoSaveWeek syncs the loss. Directly violates the disable dialog's promise:
   "Their meal data is PRESERVED — turning them back on restores everything."

2. **SOLO-2 — menu-armed paint invisibly mutates the disabled person's data.**
   `_armPaintWithMeal` gates the tapped-cell pre-queue on PERSON_ENABLED but NOT the existing-group
   pre-load; a stale manual group's disabled-person slots queue invisibly (no rendered row to un-tap);
   Assign then clears their `_userSet` ingredient pins (clearOverrides), stamps MANUAL_SET/USER_EDITED
   they never made, deletes their SKIPPED/EAT_OUT, and can write SHARED_SCHEDULE='shared' with one eater.

3. **c1 — Pick meal…/Link leftovers… silently drops the tapped cell whenever the chosen meal already
   has a link group** (auto or manual). The group pre-loads for editing; the cell the user actually
   tapped never queues; Assign no-ops on it with no feedback.

## Recommended fix order
1. CRITICALs (SOLO-1, SOLO-2, c1) — small, surgical, high harm.
2. The V253 menu-op parity MEDIUMs as one wave (shared/eo/clear op guards + preserveSel) — they share
   a root cause: the ops mirrored happy paths, not the old handlers' conditional guards.
3. Pantry-unit netting (R15-PANTRY-1/2) — decide semantics (display-unit entry?) before coding.
4. Move/swap MEDIUMs (m1/m2/m3) + solo MEDIUMs (SOLO-3/4/5, CARRY-1) + settings/sync MEDIUMs.
5. LOW sweep (dead code, docs, tombstone GC, shelf polish) as a cosmetic wave.

---

# Full findings (severity-sorted, finder evidence preserved)

### [CRITICAL] SOLO-1 — clearScheduleEdit (edit-mode Reset) permanently destroys the disabled person's plan — Apply's solo-coerced randomize never repopulates it
- ✅ **FIXED V275 (2026-07-18)**: the wipe is person-scoped to ENABLED persons only (`_clrKey` prefix check on all person-keyed maps); solo SHARED_SCHEDULE transform ('shared'/'eo-<enabled>' delete, 'eo-both'→'eo-<disabled>', 'eo-<disabled>' kept); solo leftoverLinks keep the disabled person's slots (empty groups dropped). Both-enabled behavior verified identical to the pre-fix full wipe.
- **Where**: /Users/chris/Developer/Meal Planner/index.html:14737-14743 (also 10137, 14716-14717, 4467, 5917)
- **Claim**: In solo mode, the edit-mode Reset button wipes the DISABLED person's SEL, LATE_SNACK, MANUAL_SET, SKIPPED, EAT_OUT, and USER_EDITED with no PERSON_ENABLED scoping, and the subsequent Apply randomize (coerced to the enabled person) never re-populates the disabled person's SEL. This violates the explicit V242 contract shown in the disable confirm(): 'Their meal data is PRESERVED — turning them back on restores everything' (line 5917).
- **Failure scenario**: Both enabled; Her has a full week planned (meals, a Wednesday late snack, two Set slots). User disables Her (solo-him). Later, user enters Schedule edit mode, taps Reset (confirm), makes a tweak, taps Apply. Apply randomizes Him only; Her SEL/LATE_SNACK/flags stay wiped and autoSaveWeek persists+syncs the wiped state. Weeks later the user re-enables Her expecting 'everything restored' per the disable dialog — her whole week has silently reverted to DEFAULTS and her late snack is gone, with no undo available.

### [CRITICAL] SOLO-2 — _armPaintWithMeal pre-queues a stale manual link group's DISABLED-person slots invisibly; paintAccept then overwrites their overrides/flags and can set 'shared' with one eater
- ✅ **FIXED V275 (2026-07-18)**: arm side skips `PERSON_ENABLED[sl.p]===false` slots in the group pre-load (never queued → Assign can't touch their SEL/flags/pins); accept side captures the disabled persons' slots from the meal's old group before the splice and re-attaches them to the rebuilt group (also counted toward the ≥2-cell threshold) — membership survives, data untouched. Verified: her slots un-queued, rebuilt group retains them, her SEL/MANUAL_SET byte-identical through Assign; both-enabled rebuild unchanged.
- **Where**: /Users/chris/Developer/Meal Planner/index.html:3932-3937 (also 14045-14063, 14075-14084, 4139, 10526-10528, 3183-3191)
- **Claim**: The V253 'Link leftovers…'/'Pick meal…' flow gates only the menu-cell pre-queue on PERSON_ENABLED (3940-3941) but NOT the existing-group pre-load (3932-3937). Manual link groups created before a disable retain the disabled person's slots (the validator checks raw SEL, 3190, and SEL is preserved), so arming paint queues her slots. In solo the disabled person's grid half is not rendered (10526-10528), so these queued slots are invisible and cannot be un-queued. Tapping Assign then mutates the disabled person's data: SEL ts bump, MANUAL_SET, USER_EDITED, `clearOverrides` (which deletes her `_userSet` ingredient pins — 4139: `function clearOverrides(p,d,s){delete OVERRIDES[sk(p,d,s)];}`), deletes her SKIPPED/EAT_OUT, and — when both halves of a cell are queued — writes `SHARED_SCHEDULE[ck]='shared'` (14075-14084) in a mode where sharing is supposed to be meaningless.
- **Failure scenario**: Both enabled: a manual link group exists for chicken_stir_fry — Him Mon dinner (cook) + Her Tue lunch + Her Wed lunch (leftovers), with a user ingredient pin on Her Tue lunch. Her is disabled. In solo-him edit mode the user taps Mon dinner → 'Link leftovers…' → the group pre-loads, invisibly queuing Her Tue/Wed cells (her row isn't rendered, no way to see or remove them) → user taps Assign. Her Tue lunch `_userSet` pin is deleted (clearOverrides), her MANUAL_SET/USER_EDITED/ts are stamped and synced, and Mon dinner gets SHARED_SCHEDULE='shared' while only one person is enabled. On re-enable, her pinned amounts are gone and her slots carry Set flags she never created.

### [CRITICAL] c1-pick-meal-existing-group-drops-tapped-cell — Pick meal…/Link leftovers… on a cell silently ignores the tapped cell whenever the chosen meal already has a link group (auto or manual)
- ✅ **FIXED V275 (2026-07-18)**: the `else if` became a separate `if` — the tapped cell ALWAYS pre-queues (enabled persons) alongside any existing-group pre-load. Verified: group slots + both halves of the tapped cell queued together; Assign extends the group to the tapped cell.
- **Where**: index.html:3932-3942, index.html:14045-14051, index.html:4139, index.html:3062-3066
- **Claim**: _armPaintWithMeal only pre-queues the tapped cell when getLinkGroupForMeal(mealId) returns null. Auto groups exist for EVERY multi-portion lunch/dinner batch in a randomized week (getLinkGroupForMeal checks _autoLinkGroups too, L3062-3066), so picking any currently-batched meal for a specific cell arms an edit of that meal's group elsewhere and never queues the cell the user tapped. If the user hits Assign, the tapped cell is unchanged while every OTHER group slot gets re-stamped MANUAL_SET + USER_EDITED and clearOverrides(q.p,q.d,q.s) — which wholesale-deletes OVERRIDES[sk] (L4139) including _userSet user pins and _fatBoost/freeze pins on slots the user never touched.
- **Failure scenario**: Randomized week has chicken_stir_fry cooked Wed dinner with Thu-lunch leftovers (auto group), and the user earlier pinned an ingredient amount on the Wed dinner card (_userSet override). User taps Mon dinner cell → Pick meal… → taps 'Chicken stir fry' in the protein-grouped list (it looks like a normal meal row) → paint arms with Wed+Thu highlighted, Mon NOT highlighted → user hits Assign. Result: Mon dinner still shows its old meal (the user's entire action was a no-op on the cell they acted on), while Wed/Thu get MANUAL_SET+USER_EDITED stamped and the user's Wed ingredient pin is silently deleted.

### [MEDIUM] R15-CARRY-1 — _computePriorCarryover has no PERSON_ENABLED gate — a disabled person's preserved prior-week SEL produces phantom carryover, silently corrupting Shelf display, freeze targets, Stage-2 absorb, and shopping
- ✅ **FIXED V279 (2026-07-18)**: the person loop gates on `PERSON_ENABLED[p]===false` (mirroring the skipped/eatOut gates beside it). INV3 uses the same function, so all consumers stay consistent. Verified: her-only prior-week marinara → carry present enabled, absent disabled.
- **Where**: index.html:13741 (ungated person loop) vs 4467 (the getMealId gate); consumers 5395, 7182, 8469, 9488, 12559, 17941
- **Claim**: V242's design routes ALL meal consumption through the getMealId gate ('single choke point', 4462-4467), so a disabled person cooks/shops nothing. But _computePriorCarryover reads the prior week's raw SEL directly (`priorSel[slotKey]`, 13746) for BOTH persons (13741: `['him','her'].forEach`) with no PERSON_ENABLED check — even though it DOES gate skipped and eatOut slots (13744-13745). Since SEL is deliberately preserved when a person is disabled, their prior-week crossTripCarry demand is counted even though the gate guaranteed they never ate it.
- **Failure scenario**: Both enabled; both weeks randomized (her SEL has marinara dinners in Thu-Sun). User disables Her. Week rolls over; that week becomes 'last'. Her marinara meals were never cooked (gate killed them all week), but _computePriorCarryover counts her cook2 demand → reports e.g. 1.5 cups of a jar 'open from last week' that physically does not exist. This week: Shelf shows the phantom open jar (17941), freezeTripTotals targets priorCarry+N×perUnit against it (12559), Stage-2 absorb chases it (8469/9488), and buildShoppingList depletes cook1 demand by it (5395-5416) → the shopping list under-buys marinara by up to a jar. INV3 uses the SAME function (7182), so the error is self-consistent and no invariant fires — silent under-buy.

### [MEDIUM] R15-PANTRY-1 — _pantryNetLabel subtracts in internal cache units while the shopping label (and the user's mental model) is in display units — wrong net for every pkg / produce / dry / bulkOz item
- ✅ **FIXED V278 (2026-07-18)**: pantry entries are now interpreted on the DISPLAY-unit basis (`_pantryStockInCacheUnits` converts per item class: whole produce × perWhole, oz of greens ÷ ozPerCup, DRY cups ÷ dry.ratio, containers × drained/cups; explicit 'cup'/'oz' entries still honored). An unresolvable unit returns null → gross label + honest 'have' hint (no silent wrong-basis math). All four audit scenarios reproduce correct: peppers 2 (was 3), 1 dry cup covers 2.5 cooked, 1 tofu container credits 14 oz (was 1 oz), spinach no longer under-buys.
- **Where**: index.html:4869-4877 (netting), 4890-4901 (produce label), 4930-4938 (dry label), 4947-4962 (pkg label)
- **Claim**: The subtraction `net = qtyObj.num - PANTRY[dbKey].qty` runs on the CACHE unit (cups for produce/dry/bulkOz, oz/cups for pkg items), but shopQtyWithCount then converts that number into a completely different display unit (whole peppers, containers, dry cups, oz). The comment says blank pantry unit = "the item's natural unit", but the natural unit the user sees on the shopping list is the DISPLAY unit, not the cache unit. So a blank-unit pantry entry is subtracted on the wrong basis for every item in these classes; and the unit-match guard can't save the dry class because both bases are literally named 'cup'.
- **Failure scenario**: PANTRY_SUBTRACT on. (a) Shopping needs 4.8 cups bell pepper ("4 bell peppers"). User adds pantry 'bell pepper' qty 2, blank unit — meaning 2 peppers (=2.5 cups), because the shopping list only ever showed them pepper counts. Code nets 4.8−2 = 2.8 cups → "3 bell peppers"; correct is 4.8−2.5=2.3 → "2 bell peppers" — over-buy. (b) User owns 1 cup DRY rice, adds 'brown rice cooked' qty 1 unit 'cup' (matches su='cup', guard passes); code subtracts 1 COOKED cup ≈ 0.4 dry cup instead of 1 dry cup → net label over-states dry cups to buy ~2.5×. (c) User owns a whole 16oz tofu container, adds qty 1 blank unit; code subtracts 1 oz from a 20oz demand → label still "2 containers (16oz)" — the netting is silently inert and the user (seeing '🏠 −1') believes their container was counted. (d) Reverse direction: baby spinach (bulkOz, cache unit cup, display oz) — pantry qty 2 meaning 2 oz (~0.7 cup) subtracts 2 CUPS → net label UNDER-states oz to buy → under-buy.

### [MEDIUM] R15-PANTRY-2 — Pantry stock is netted from cook1, cook2, AND custom trip views simultaneously — the same physical stock is promised to every trip, under-buying the later trip
- ✅ **FIXED V278 (2026-07-18, user-picked sequential-depletion model)**: stock covers cook1's demand first; only the remainder nets cook2's label (`_pantryNetInfo` takes the trip + a cook1 gross-demand map built via a suppressed recursive `buildShoppingList('cook1','both')`). The custom 'Shop Now' list nets full stock (an immediate shop with what's on hand — an alternative to cook1, not a third consumer). Verified: 2-cup stock vs 3+3 demand → cook1 nets 2, cook2 nets 0; 0.5-cup cook1 demand → cook2 gets the remaining 1.5.
- **Where**: index.html:5512-5526 (per-trip grouped loop, netting applied per view with no trip attribution or depletion)
- **Claim**: _pantryNetLabel has no trip parameter and PANTRY has no depletion mechanism, so buildShoppingList nets the identical pantry qty out of the Both-view label of EVERY trip (cook1, cook2, and the custom 'Shop Now' list — the netting sits in the grouped-items loop shared by all three branches). One cup of stock can only cover one trip's demand, but both trips' labels each assume they get it. This is the trip-level analog of the M22 'one physical pantry can't be split across views' lesson that WAS applied to Him/Her views (per-person views stay gross) but not across trips.
- **Failure scenario**: PANTRY_SUBTRACT on. Pantry: 'chicken broth' qty 2 (cups, matching unit). cook1 needs 3 cups → Both view shows net 1 cup; cook2 needs 3 cups → Both view ALSO shows net 1 cup. User shops Mon–Wed buying 1 cup-equivalent and cooks — the 2 pantry cups are now consumed — then shops Thu–Sun trusting the still-netted '1 cup' label → 2 cups short for the week. Nothing prompts the user to decrement the pantry entry between trips, and the custom Shop Now list nets the same stock a third time.

### [MEDIUM] R15-SETTINGS-1 — Unchecking the currently-active tab's visibility inside the settings panel force-closes the panel mid-interaction — and falsifies the documented 'topTab never mutates while the panel is open' invariant
- ✅ **FIXED V279 (2026-07-18)**: with the panel open, `applyTabPrefs` retargets `topTab='meals'` silently (no setTopTab, panel stays up; the tab button still hides); closing the panel later lands on Meals via the existing `setTopTab(topTab)`. The toggleSyncPanel invariant comment documents the exception. Panel-closed path unchanged.
- **Where**: index.html:5896-5898 (applyTabPrefs fallback) + 5946 (setTopTab hides syncView) + 19249-19251 (the invariant comment)
- **Claim**: toggleSyncPanel's close branch is built on 'topTab was never changed while the panel was open, so it IS the previous tab' (19250-19251). But setTabPref → applyTabPrefs runs while the panel is open, and if the pref being disabled matches topTab, it calls setTopTab('meals') — which mutates topTab AND executes `document.getElementById('syncView').style.display='none'` (5946), yanking the settings panel out from under the user the instant they tap the checkbox. The subsequent renderSyncView() in setTabPref (5900) then re-renders an already-hidden panel.
- **Failure scenario**: User is on the 🛒 Shopping tab, taps ⚙️ (panel opens, topTab stays 'shop'), unchecks '🛒 Shopping' intending to also uncheck '🧺 Shelf' next. The panel instantly disappears and the Meals view renders. The user must find and re-tap the gear to finish what they were doing; to them the settings screen 'crashed'. Expected behavior per the panel's own design comment: the panel stays open, only the tab button hides, and closing later falls back gracefully.

### [MEDIUM] R15-SHELF-1 — switchWeek never re-renders the Shelf tab — week pills update but the Shelf keeps showing the previous week's carryover/freezer/staples data
- ✅ **FIXED V279 (2026-07-18)**: `switchWeek` gained the `if(topTab==='shelf')renderShelf()` branch (after `activeWeek` updates, so the V244 settings-close path's stale render is also corrected). Verified via render-call spy across a week switch.
- **Where**: index.html:18596-18597
- **Claim**: renderShelf is week-dependent (buildShoppingList for the active week at 17952-17953 plus _computePriorCarryover(_prevWeekKey()) at 17941, where _prevWeekKey reads activeWeek). switchWeek re-renders meals unconditionally and shop conditionally, but has no shelf branch, so a week-pill tap while the Shelf tab is open leaves stale content on screen under a newly-highlighted week pill. The week bar IS shown on the Shelf tab (18602 themes it), so this path is fully reachable. The same gap applies via the V244 settings-panel close inside switchWeek (18521-18525): setTopTab(topTab) renders the shelf for the OLD week, then the switch proceeds without re-rendering.
- **Failure scenario**: User is on 🧺 Shelf viewing This week (shows 'Open from last week: marinara 1.5 cups', this week's freezer usage). Taps the Next week pill. The pill highlights Next, but the Shelf still shows This week's numbers — including 'use in Mon–Wed cooking' guidance computed from the wrong week pair ('this'+'last' instead of 'next'+'this'). The user plans next week's fridge usage from the wrong week's data until they happen to tap away and back.

### [MEDIUM] R15-SYNC-1 — Force Push does not delete receiver-only pantry entries — the pantry merge iterates only remote keys, unlike the weekData merges the R14-M3 floor was built for
- ✅ **FIXED V279 (2026-07-18)**: the pantry merge walks the UNION of remote+local keys (adjTargets pattern) — a receiver-only entry resolves against the Force Push floor and is deleted; normal pulls (floor 0) never delete; a local entry edited after the force ts survives (per-key floor semantics). All 5 merge cases verified via synthetic mergeSyncData payloads.
- **Where**: index.html:18945-18953 (pantry merge) vs 18905-18916 (union-key pattern with floor-driven deletion)
- **Claim**: R14-M3's forcePushTs floor exists so that keys only the RECEIVER has also resolve remote-wins and get deleted ('replace its state with yours', comment 18665-18675; confirm dialog 19137 promises the same). The pantry merge applies the floor to the timestamp comparison (18948) but only walks Object.keys(remote.pantry) — a pantry entry that exists only on the receiver is never visited, so it survives a Force Push and then re-propagates outward on the receiver's next regular sync. The adjTargets merge shows the correct pattern: it builds a union key set (18905-18908) so `remAdj[k] === undefined` → delete (18912-18913). CLAUDE.md's Shelf section states pantry sync follows 'the customIngredients pattern … forcePushTs floor applies' — the floor applies only to keys present in the remote map.
- **Failure scenario**: Phone B adds pantry entry 'oats' and never syncs. Phone A (which has no 'oats') hits Force Push — the dialog says B's 'subsequent Pull will replace its state with yours'. B pulls: every weekData key, skip, SEL, and late snack is replaced per M3, but 'oats' survives. B's next regular Sync (pull-merge-push) writes 'oats' into the gist; A's next pull imports it via the `!lE` branch. The entry the force push was supposed to eliminate resurrects on both phones. (Soft-deleted qty:0 tombstones only cover entries BOTH sides have seen; receiver-only live entries are exactly the un-covered case.)

### [MEDIUM] SOLO-3 — V253 menu ops' un-share cascades strip the DISABLED person's MANUAL_SET/USER_EDITED/EAT_OUT via stale 'shared' cells
- ✅ **FIXED V279 (2026-07-18)**: three layers gated on the person being ENABLED — `_v70UndoEatOutSide` early-returns for disabled persons (defense in depth for every caller), `_schedOpShared`'s un-share branch clears flags per enabled side only, and `_v85ClearSideVisualIfAny`'s shared branch gates the OTHER side's flag/EAT_OUT deletes and edit-mode stamps (incl. the cleared-link-side stamp, which feeds Apply's persistent group-membership removal). SHARED_SCHEDULE cell-state adjustments unchanged. Verified: stale-shared cell + her pins survive 'Him eats out', 'Clear', and direct V70; both-enabled behavior identical.
- **Where**: /Users/chris/Developer/Meal Planner/index.html:3866 + 3851-3856 (also 3543-3557 via 3884-3885)
- **Claim**: Stale SHARED_SCHEDULE='shared' entries survive a person-disable (setPersonEnabled at 5912-5926 cleans nothing, and DEFAULTS historically marks dinners shared), and `_schedCellShared` (3828-3831) still reports them. Two solo-reachable menu ops then run both-side cascades with no PERSON_ENABLED gate: (a) 'Him/Her eats out' ON — `_schedOpEO` line 3866 `if(_schedCellShared(d,s)) _schedOpShared(d,s,false)` whose off-branch (3851-3856) deletes USER_EDITED and MANUAL_SET for BOTH persons; (b) 'Clear' — `_schedOpClear` gates the per-side V85 loop (3884-3885) but the V85 shared branch itself (3541-3557) deletes the OTHER (disabled) side's USER_EDITED/MANUAL_SET and non-customized EAT_OUT. The disabled person's Set/eat-out state — data the disable dialog promises is preserved — is silently mutated.
- **Failure scenario**: Her had Monday dinner Set (MANUAL_SET+USER_EDITED, a meal she picked) and the cell carries SHARED_SCHEDULE='shared' from the default schedule intent. She is disabled. Solo-him user opens the Mon-dinner cell menu and taps 'Him eats out' (or 'Clear'). The stale shared state routes through the un-share cascade and deletes HER MANUAL_SET/USER_EDITED. She is re-enabled next month; the next randomize sees her slot unlocked and re-rolls the meal she had explicitly Set — her manual choice is lost with no user action ever taken on her plan.

### [MEDIUM] SOLO-4 — LATE_SNACK is never gated on PERSON_ENABLED — a disabled person's late snack keeps being shopped, counted, INV20-scored, and can trigger an unresolvable partner-waste dialog
- ✅ **FIXED V279 (2026-07-18)**: new `_lateSnackFor(p,d)` accessor (null for disabled persons) at EVERY consumption-side read — shopping trip path + carry pool + custom path, INV3 expected map + carry mirror, weekly INV20 carry site, calcTotals + INV2/INV17/balancer-dailyMacros mirrors, computeDailyFV, and the harness expected map — so paired checkers stay consistent. The entry itself is preserved for re-enable. Partner-waste dialog exposure dies with the shopping contribution. Verified: her 0.5c cottage-cheese snack in shopping with her enabled, absent disabled; solo verify after re-randomize = 0 hard fires. Known residual (LOW-sweep): the custom shop grid can still RENDER a disabled person's late-snack row (getShopPersons ungated) — checking it is now inert at shop time.
- **Where**: /Users/chris/Developer/Meal Planner/index.html:5292-5307 (also 8717-8719, 7099-7114, 8278, 14305-14307)
- **Claim**: The V242 gate lives solely in getMealId (4467), but LATE_SNACK is keyed pk(p,d) and read directly everywhere: buildShoppingList adds the disabled person's late-snack ingredients to every cook1/cook2 list (5292-5307) and to the custom-trip path (5181-5186); calcTotals counts them (8717-8719); INV3's expected map includes them (7099-7114, consistent so no fire); and verifyInvariants' INV20 site reads buildShoppingList(trip,'both') (8278), so a disabled person's pkg/produce late-snack item can produce a hard INV20 fire that the solo retry selector never sees (`_measuredTripWasteForPersons` uses the single-person view, 13585) and that freezeTripTotals cannot fix (late snacks are not balanced-cache slots). `_detectPartnerWasteFixables` (14305-14307) counts that contribution as `otherNum > 0.001`, so the partner-waste dialog can fire in solo offering to 'Adjust Her's amounts' — the amounts-only re-freeze can't touch a late snack, so the '⚠️ Could not fully fit' toast recurs on every randomize. The disabled person's tab is hidden, so the late snack cannot be viewed, edited, or deleted without re-enabling them. This contradicts the disable dialog's 'the planner stops cooking, shopping, and scoring for them.'
- **Failure scenario**: Her has a Saturday late snack with DB-keyed ingredients (e.g. 0.5 cup cottage cheese — a pkg container item). Her is disabled. Solo-him randomize completes; verifyInvariants fires INV20 hard for the cottage-cheese trip waste caused entirely by her invisible late snack, and the shopping list keeps a 'Her' contribution the household will never eat. If the item passes the partner-waste gates, every randomize/Apply pops 'Package waste in Her's plan → Adjust Her's amounts', which always ends in the 'Could not fully fit' toast because freeze cannot modify late snacks. The user has no UI path to remove the snack without re-enabling Her.

### [MEDIUM] SOLO-5 — getRecentMealIds reads raw SEL for both persons — a disabled person's preserved plan phantom-blocks the enabled person's variety, with former leftovers misclassified as cooks
- ✅ **FIXED V279 (2026-07-18)**: all four windows (same-day, backward, prev-week tail, forward) iterate `['him','her'].filter(PERSON_ENABLED)` — a disabled person's preserved SEL no longer blocks the enabled person's picks. Both-enabled behavior byte-identical (Test 1 gate). Verified: her-only miso_tofu excluded from the recent set in solo, included when enabled.
- **Where**: /Users/chris/Developer/Meal Planner/index.html:13821-13835 (also 13842, 13853-13880, 13891, 4514)
- **Claim**: getRecentMealIds deliberately reads `SEL[sk(pp, d, s)]` directly (13824) for BOTH persons with no PERSON_ENABLED gate, in the same-day, backward, prev-week-tail, and forward windows. In solo mode the disabled person's preserved SEL entries are all still counted as recent household cooks. Worse, the leftover exclusion (13826-13827: `const lo = leftovers[sk(pp, d, s)]; if (lo && lo.isLeftover) return;`) is dead for the disabled person because computeLeftovers excludes them entirely via getMealId (4514) — so even slots that were leftover portions when they were enabled now count as fresh cooks. Every such phantom entry blocks the enabled person's Phase-1 picks and swap candidates (rerollMissDays, 3-A/3-C, _trySwapForWaste, _absorbFindCandidates all use this filter) for a ±4-day window around meals nobody will cook. Same phantom class as R14-M8/M9/M10; direction is over-blocking only (no invariant can fire), but it silently shrinks the solo candidate pool for the entire week the stale SEL persists.
- **Failure scenario**: Her week (preserved) contains salmon_stir_fry cooked Tue with leftovers Wed/Thu. Her is disabled Monday. Solo-him randomize: getRecentMealIds treats her Tue cook AND her Wed/Thu leftover entries as three separate recent-cook signals, blocking him from picking salmon_stir_fry anywhere Mon–Sun (each phantom blocks ±4 days) even though nothing will be cooked for her. Multiplied over her 14+ preserved lunch/dinner entries, the solo Phase-1 pool is materially narrowed, pushing picks into the fallback cascade and degrading goal-hit/waste outcomes versus a true one-person household.

### [MEDIUM] c2-prequeue-clears-partner-skip-eatout — Pre-queue unconditionally queues BOTH enabled persons — Assign silently cancels the other person's SKIPPED/EAT_OUT, overwrites their differing meal, and force-shares the cell
- ✅ **FIXED V277 (2026-07-18)**: the implicit tapped-cell pre-queue skips persons with SKIPPED, per-slot EAT_OUT, or eo-* state at the cell. An explicit paint tap on that half still works (V45 semantics preserved for deliberate actions).
- **Where**: index.html:3938-3941, index.html:14052-14063, index.html:14075-14084, index.html:3915
- **Claim**: _armPaintWithMeal pre-queues him AND her at the tapped cell with no check of SKIPPED, EAT_OUT, or MANUAL_SET. paintAccept then (V45 semantics, designed for cells the user EXPLICITLY tapped) deletes SKIPPED/EAT_OUT on every queued slot, adjusts eo-* SHARED_SCHEDULE state, and — because both halves are queued at one (d,s) — writes SHARED_SCHEDULE='shared'. Under the removed picker flow the user had to tap each half individually; V253 makes both-person assignment (and partner skip/eo cancellation) the zero-extra-tap default of the single-cell 'Pick meal…' action. schedMenuLink (L3915) has the same exposure: arming him's meal pre-queues her, so her differing meal gets overwritten on Assign.
- **Failure scenario**: Her Tuesday dinner is Skipped via the card pill (she's fasting); him has a normal dinner. User taps the Tue dinner cell → Pick meal… → picks a new meal (no existing group) → both halves auto-highlight → Assign. Her skip is silently deleted, she is assigned the meal, and the cell becomes SHARED_SCHEDULE='shared' — her calorie day now includes a dinner she explicitly skipped, and the daily redistribution she relied on is undone without her half ever being tapped.

### [MEDIUM] c3-link-meal-him-first-ambiguity — schedMenuLink resolves the cell's meal as getMealId('him')||getMealId('her') — on mixed cells her meal is never linkable and him's meal wins silently
- ✅ **FIXED V277 (2026-07-18)**: mixed cells (him ≠ her, both enabled) open a 'Link whose meal?' chooser (`_schedMenuView='linkpick'`, `schedMenuLinkPick`); Link leftovers… arming carries `linkOnly:true`, scoping the tapped-cell pre-queue to sides currently EATING the linked meal — linking her chain can no longer overwrite him's different meal.
- **Where**: index.html:3915, index.html:4011-4012
- **Claim**: Both the render gate (L4011) and the action (L3915) use `getMealId('him',m.d,m.s)||getMealId('her',m.d,m.s)`. When him and her have DIFFERENT meals at the cell (common for non-shared lunches), 'Link leftovers…' always arms paint with him's meal; there is no way to link her's meal from that cell. Combined with c2's pre-queue, accepting the default converts her's meal to him's.
- **Failure scenario**: Him Tue dinner = salmon_stir_fry, her Tue dinner = miso_tofu (not shared). User wants her miso_tofu to feed her Wed lunch as leftovers, taps the Tue dinner cell → Link leftovers…. Paint arms with 'Assigning: Salmon stir fry' and pre-queues BOTH halves (salmon has no group). If the user taps her Wed lunch and hits Assign, her Tue dinner is rewritten to salmon and a salmon group forms — the opposite of the intent; her miso_tofu chain is unreachable from the menu.

### [MEDIUM] c4-breakfast-cell-group-edit-gating-hole — Link leftovers… is hidden on breakfast cells, but Pick meal… on a breakfast cell lists all lunch/dinner link groups — queuing the breakfast cell creates a group the detector ignores and a lying 'cook day' visual
- ✅ **FIXED V277 (2026-07-18)**: `_schedMenuMealListHtml` gates the 'Linked groups' section to lunch/dinner cells (the detector's cookSlots) — breakfast cells list no group rows, closing the group-edit entry point the L4012 gate missed.
- **Where**: index.html:3956-3965, index.html:4012, index.html:4502, index.html:4538, index.html:4548, index.html:3027-3029, index.html:3071-3079
- **Claim**: The L4012 gate deliberately hides Link leftovers… for breakfast cells, but _schedMenuMealListHtml's 'Linked groups — tap to edit' section (L3956-3965) has no slot filter — on a breakfast cell it lists every lunch/dinner group. Tapping one arms group-edit paint; every grid cell is paintable in paint mode, so the user can queue their breakfast cell into the group and Assign. The detector unconditionally excludes breakfast from batches (cookSlots/leftoverSlots = ['lunch','dinner'], L4502; anchor gate L4538; leftover gate L4548), so the breakfast slot cooks the dinner meal as a FULL separate solo cook (shopping buys full extra ingredients) while the manual group renders it as a linked leftover. Worse, isLinkAnchor uses _dayslotKey where breakfast=0 sorts before lunch/dinner (L3027-3029), so if the breakfast cell is on the earliest group day it becomes the SOLID 'cook day' cell (V258: bright = the day you act) while the real cook is the lunch/dinner anchor.
- **Failure scenario**: Week has a marinara_chicken group anchored Mon dinner with Tue dinner leftover. User taps Mon BREAKFAST cell → Pick meal… → taps the marinara_chicken group row → paint arms; user taps their breakfast cell (their point of entry) and Assign. SEL[him_Monday_breakfast]=marinara_chicken; the grid now paints Mon breakfast as the SOLID cook block (earliest _dayslotKey) and Mon dinner as a leftover wash — but computeLeftovers still anchors the batch at Mon dinner, breakfast cooks a full separate portion, and the shopping list quietly grows by a full extra recipe (plus its marinara demand, perturbing the jar-boundary freeze/INV20 accounting at next randomize).

### [MEDIUM] c5-apply-undo-reset-leave-paint-armed — Apply/Undo/Reset while menu-armed paint is active never tear down paint state — painting survives edit-mode exit, contradicting V253's 'painting is edit-mode-only' and creating a no-undo commit path
- ✅ **FIXED V277 (2026-07-18)**: `_disarmPaint()` (clears _paintMode/_paintMealId/_paintQueued/_paintExistingColorIdx) runs at the entry of applyScheduleEdit, undoScheduleEdit, and clearScheduleEdit — uncommitted paint dies with the edit session. Harness paint ops unaffected (all commit via paintAccept before any lifecycle call — verified).
- **Where**: index.html:14611-14727, index.html:14764-14779, index.html:14729-14762, index.html:10136-10140, index.html:10536, index.html:14021
- **Claim**: The edit-mode header buttons Reset/Undo/Apply render whenever _scheduleEditMode is true, with no _paintMode gate (L10136-10140), so they are reachable while paint is armed from the menu. applyScheduleEdit (L14706 sets _scheduleEditMode=false), undoScheduleEdit (L14768), and clearScheduleEdit contain zero references to _paintMode/_paintQueued/_paintMealId (verified by grep — no _paint* identifier appears in L14611-14779). After Apply or Undo, edit mode is off but paint stays armed: the grid still renders paint taps (isPaintCol=_paintMode, L10427), the paint bar still shows Assign (L10536), and paintAccept's only guard is `if(!_paintMode) return;` (L14021) — so Assign commits SEL/MANUAL_SET/SHARED_SCHEDULE directly to live state OUTSIDE edit mode with no snapshot to Undo. V30/V147 protect week switches during edit/paint, but the edit-mode lifecycle buttons got no equivalent for V253's menu-armed paint.
- **Failure scenario**: In edit mode the user taps a cell → Pick meal… → chooses a meal (paint arms, both halves queued). They change their mind about the whole edit session and hit Undo, believing everything is cancelled. Edit mode exits and the snapshot is restored — but the paint bar is still up with the meal armed. A stray tap on Assign now writes SEL + MANUAL_SET + USER_EDITED + SHARED_SCHEDULE='shared' straight into live state (autoSaveWeek persists it) with no edit-mode snapshot; the 'Undo' the user just performed cannot revert it, and clearOverrides wipes any freeze/user pins on the queued slots.

### [MEDIUM] m1-movepending-survives-week-switch — _movePending survives switchWeek — armed move completes against a different week's slots (including the View-Only last week)
- ✅ **FIXED V279 (2026-07-18)**: `switchWeek` silently cancels an armed move alongside the other week-less transients (no confirm — unlike paint, an arm carries no accumulated work).
- **Where**: index.html:18517-18596 (switchWeek), index.html:10953 (_movePending), index.html:10971-10977 (completeMoveMeal)
- **Claim**: switchWeek explicitly clears every other week-scoped transient (_paintQueued V147, SHARED_UNDO V161, edit mode V30) precisely because their keys are sk(p,d,s) with no week component — but _movePending ({p,d,s}, same week-less shape) is never cleared anywhere except cancelMoveMeal/completeMoveMeal. The only references to _movePending in the whole file are lines 9954-9961, 10953-10976, and the CSS-exempt pill class; switchWeek, randomizeWeek, mergeSyncData, applyScheduleEdit, and setPersonEnabled never reset it.
- **Failure scenario**: User arms ⇄ on This Week's him_Monday_dinner ("move my Monday curry"), then taps the "Last" week pill to check something. Last week's cards render with "Moving… ✕" on ITS Monday dinner and ⤓ Here on eligible cards. Tapping ⤓ Here on last week's Wednesday dinner swaps LAST week's Monday/Wednesday meals — mutating the nominally View-Only week (SEL+overrides+MANUAL_SET+ts bumps, synced outward via _bumpSlotTs) — while the user believed they were still moving this week's meal. Same wrong-week swap occurs with 'next'.

### [MEDIUM] m2-skip-source-midarm-stuck-ui — Skipping/eating-out the SOURCE slot mid-arm removes the only cancel affordance, then ⤓ Here taps silently no-op and leave dead pills on screen
- ✅ **FIXED V279 (2026-07-18)**: three layers — (a) toggleSkip/toggleEatOut on the armed SOURCE disarm the move at the root; (b) `_moveEligible` validates the source's SKIPPED/EAT_OUT too, so ⤓ pills drop on the next render; (c) `completeMoveMeal` on a stale/failed target disarms + re-renders instead of silently no-oping (dead pills can no longer persist). Verified: skip-disarm, eo-disarm, stale-tap cleanup, failed-swap cleanup, and a legit move still swapping.
- **Where**: index.html:9953 (pill gate), index.html:10955-10967 (_moveEligible dst-only skip check), index.html:10971-10976 (disarm-before-swap), index.html:10982-10988 (cache abort), index.html:6127/6157 (skipped slots get no cache entry)
- **Claim**: (a) _moveEligible checks SKIPPED/EAT_OUT only on the DESTINATION key — the source's skip/eo state is validated only implicitly at arm time (the ⇄ pill gate) and never re-checked. (b) While armed, the source card's Skip and Eat Out pills remain tappable; toggling one makes line 9953's gate suppress the "Moving… ✕" pill while _movePending stays armed, and no ⇄ renders anywhere else (line 9958 requires !_movePending) — the user has NO cancel affordance. (c) Tapping any ⤓ Here then runs completeMoveMeal, which nulls _movePending BEFORE swapMeals; swapMeals aborts at the cache-hole check (skipped slots are never built into _dayBalancedCache) and returns false BEFORE its renderMeals call — so the screen keeps showing green ⤓ Here pills that are now permanently dead (tapping again hits "if(!_movePending) return;").
- **Failure scenario**: User arms ⇄ on him_Monday_lunch, changes their mind and taps Skip on that same card (visible in the expanded pill row). Card turns skipped; Moving…✕ vanishes; every eligible card still shows ⤓ Here with no way to cancel. User taps ⤓ Here on Wednesday lunch expecting the move: nothing happens (silent abort, no re-render), and the ⤓ Here pills remain frozen on screen doing nothing until some unrelated action re-renders. If instead the source slot's cache entry survived (any future refactor of the invalidation), the swap would complete INTO a skipped slot — the exact state the destination gate exists to prevent.

### [MEDIUM] m3-identical-meal-swap-destructive-noop — No idA===idB guard: 'swapping' two slots holding the SAME meal silently exchanges portion amounts/pins, dissolves the shared cell, and locks both slots
- ✅ **FIXED V279 (2026-07-18)**: `_moveEligible` rejects same-meal destinations (no ⤓ pill renders — including the shared-cell twin, the worst case) and `swapMeals` gained the `idA===idB` backstop so no caller can reach the exchange. Verified both layers + the shared-twin case on a live randomized week.
- **Where**: index.html:10978-10981 (missing guard), index.html:10989-10997 (overrides/cache exchange), index.html:11000-11005 (shared dissolve), index.html:10955-10967 (_moveEligible has no same-meal check)
- **Claim**: swapMeals only rejects !idA||!idB||ka===kb — two different slots holding the same meal id pass every gate. For such pairs the SEL writes are no-ops, but the OVERRIDES and balanced-cache rows still exchange, 'shared' cells still dissolve, and both slots get MANUAL_SET+USER_EDITED. The most damaging instance is the two sides of one shared cook: him_Mon_dinner ↔ her_Mon_dinner is fully eligible (different sk key, same slot group, meal available to both persons — ⤓ Here renders on her card while armed from his), and completing it swaps his 2800-kcal-prop portion amounts with her 1900-kcal-prop amounts while visibly 'moving' nothing. It also evades the R14-C6 Reset guard: _postRandomizeSel still matches (meal id unchanged), so ↺ Reset on one slot literal-restores its pre-swap snapshot row while the twin keeps the swapped row — the batch now holds two copies of one person's portion.
- **Failure scenario**: Shared Monday dinner (one pot, him 62%/her 38% kcal-prop split). User taps ⇄ on his card, then — exploring — taps the green ⤓ Here that appears on HER Monday dinner card. Outcome: meal names change nowhere, but his card now shows her-sized amounts (~35% smaller) and hers shows his; the green Shared schedule state is deleted; both slots are MANUAL_SET so the next randomize preserves the mis-sized, no-longer-marked-shared pair. A later ↺ Reset on his slot restores his original row while her slot keeps his old row — household totals for the batch now double-count his portion.

### [MEDIUM] v253-clear-rerolls-default-and-autoshared-cells — _schedOpClear marks cells touched even when nothing was cleared (default and auto-shared cells) with no preserveSel — Apply rerolls their meals
- ✅ **FIXED V277 (2026-07-18)**: touched/preserveSel stamps only fire when the V85 loop actually cleared something; a pure auto-shared cell gets the old center-tap treatment (touched + preserveSel both sides — visual drops via V81 intent-render, meals kept). Default cells: Clear is a true no-op.
- **Where**: index.html:3879-3889 (op), index.html:3538 (V85 no-op return), index.html:14685-14694 (touched → no tempLock → reroll)
- **Claim**: `_schedOpClear` unconditionally stamps `_scheduleEditTouchedCells` + `_scheduleEditTouchedSides` for both sides (L3886-3888) even when `_v85ClearSideVisualIfAny` returned false immediately (default cell: L3538 `if(!hasLink && !hasShared && !hasMyEatOut && !hasManualPurple && !hasSkip) return false;`). Touched sides without a preserveSel stamp skip the temp-lock in Apply's diff (`|| !!_scheduleEditTouchedSides[k]` at L14690) and are re-rolled by the Apply randomize. Two sharp cases: (1) already-default cells inside a day/column Clear get new random meals while stateful cells in the same Clear KEEP their meals (V85's shared/skip/purple/eo branches all stamp preserveSel) — inverted, surprising inconsistency; (2) an AUTO-shared cell (green glyph, no stored state, no pins) hits NO V85 branch (V85 doesn't count auto-shared, L3530), so Clear rerolls BOTH sides' meals, whereas the old gesture for clearing an auto-shared visual (center tap, L3660-3678) stamped preserveSel on both sides and kept the meals.
- **Failure scenario**: User is on the Shared tab, opens the Dinner column menu and taps 'Clear' intending to reset the column's states. Three dinners had shared/eo state (cleared, meals preserved via preserveSel); the other four dinners were already plain — all four get marked touched with no preserveSel, and Apply's randomize replaces those four meals with new random picks. Same for a single auto-shared cell: Clear on it replaces both people's current meal instead of just dropping the green visual (the old center-tap behavior). Loop-termination itself is fine (each V85 branch consumes its layer; ≤4 iterations, guard 10 never binds).

### [MEDIUM] v253-eo-on-autoshared-clears-other-side — _schedOpEO(on=true) on an AUTO-shared cell invokes the un-share cleanup, deleting the OTHER person's MANUAL_SET/USER_EDITED where the old tap did not
- ✅ **FIXED V277 (2026-07-18)**: the INV24 pre-clear now checks explicit stored `SHARED_SCHEDULE==='shared'` only (mirrors V85's check) — auto-shared cells route straight to the eo write with the other side's flags untouched.
- **Where**: index.html:3862-3869 (op), index.html:3828-3831 (_schedCellShared counts auto-shared), index.html:3530 (V85 counts only explicit shared), index.html:3850-3856 (flags deleted both sides)
- **Claim**: `_schedOpEO` on-branch: `if(_schedCellShared(d,s)) _schedOpShared(d,s,false);`. `_schedCellShared` (L3828-3831) counts `_autoSharedCells` (same-meal detection with NO stored SHARED_SCHEDULE entry). `_schedOpShared(false)` then deletes USER_EDITED and MANUAL_SET on BOTH sides (L3851-3854) and marks both sides touched. The old equivalent (schedTapLeft) used _v85ClearSideVisualIfAny whose shared check is `const hasShared = (cur === 'shared');` (L3530) — auto-shared cells were NOT counted, so tapping 'him eats out' on an auto-shared cell advanced him to eo-him and never touched her's flags. The menu op therefore clears the other person's manual pin for a one-sided action on any cell where both sides happen to have the same meal (the most common shared shape after a randomize).
- **Failure scenario**: Him and her both have the same pinned lunch on Tuesday (her pinned it via card dropdown → MANUAL_SET[her_Tuesday_lunch]; the cell renders auto-shared, no SHARED_SCHEDULE entry). Edit mode → cell menu → 'Him eats out' → Apply. Her side: MANUAL_SET deleted by the op, then preserveSel→tempLock→`delete MANUAL_SET` at L14720 leaves her slot unpinned post-Apply. Next Randomize rerolls her pinned meal and clears her ingredient overrides. Old tapLeft flow left her MANUAL_SET fully intact (diff loop returned at `if(MANUAL_SET[k]) return;`).

### [MEDIUM] v253-shared-on-unconditional-v70 — _schedOpShared(on=true) runs the V70 cascade unconditionally, stripping MANUAL_SET/USER_EDITED/SKIPPED from non-eat-out cells — pinned slots lose their pin after Apply
- ✅ **FIXED V277 (2026-07-18)**: the V70 cascade runs per-side, ONLY for sides actually in an eat-out state (stored eo-* or per-slot EAT_OUT flag — the latter also closes an INV24 shared∧EAT_OUT window). Clean cells mirror schedTapCenter's normal→shared branch: SHARED_SCHEDULE alone, pins/skips intact.
- **Where**: index.html:3840-3846 (op), index.html:3499-3509 (_v70UndoEatOutSide), index.html:3689-3692 (old mirror), index.html:14654,14662-14666,14720 (Apply consequences)
- **Claim**: The op's comment says the V70 clear is for the 'eo → shared' transition, but _v70UndoEatOutSide runs on BOTH sides for EVERY shared-on call, including plain normal, auto-shared, and already-explicit-shared cells (bulk mixed scope). The old handlers it claims to mirror — schedTapCenter's normal→shared branch (L3689-3692: `else { SHARED_SCHEDULE[k]='shared'; }`) and setSharedPreset (L4030-4034) — wrote only SHARED_SCHEDULE. _v70UndoEatOutSide unconditionally deletes USER_EDITED[k], MANUAL_SET[k], SKIPPED[k], bumps the sync ts, and stamps _scheduleEditPreserveSel. Because MANUAL_SET is deleted, Apply's diff loop no longer short-circuits at `if(MANUAL_SET[k]) return;` (L14654); instead the preserveSel branch temp-locks the slot (L14662-14666) and the post-randomize cleanup `tempLocked.forEach(function(k){ delete MANUAL_SET[k]; })` (L14720) strips the lock that setSchedule legitimately re-stamped. Old flow: a pinned slot kept MANUAL_SET through share+Apply (diff loop returned early, slot never entered tempLocked). New flow: post-Apply the slot has USER_EDITED=true but MANUAL_SET=false.
- **Failure scenario**: User pins him_Monday_dinner via the card dropdown (MANUAL_SET+USER_EDITED set, ingredient _userSet pins present). Edit mode → tap the Mon/Dinner cell → 'Shared meal' → Apply. Post-Apply MANUAL_SET is false on both sides (old flow: true). The NEXT Randomize click treats the slot as unlocked: Phase-1 clearOverrides wipes the slot's overrides (including user ingredient pins, which only MANUAL_SET slots are skipped for) and the reroll can replace the user's pinned meal — silent loss of a manual pin after a routine 'make it shared' action. Also mid-edit the Set purple arcs vanish immediately (USER_EDITED deleted), and if a side had a card-pill Skip, it is silently deleted.

### [MEDIUM] v253-state-checks-dont-mirror-grid — _schedCellShared/_schedCellEO do not actually mirror the grid's render conditions — menu checkmarks contradict the visible cell state and invert the toggle direction
- ✅ **FIXED V277 (2026-07-18)**: rewritten to truly mirror the grid — `_schedCellShared` implements the V87 cleared-suppress, V81 touched-cell intent, and V76 mealsMatch (+ neither side skipped/red) branches; `_schedCellEO` (via new `_schedCellSideRed`) counts eo-* state OR per-slot EAT_OUT, with SKIPPED rendering-grey priority. Menu ✓s now agree with the visible cell state, so toggle direction follows what the user sees. (Known parity limit kept: a CUSTOMIZED eat-out is preserved by V70 on toggle-off, so its red — now honestly ✓-checked — clears only via the card pill, as in the old flow.)
- **Where**: index.html:3827-3835 (checks + comment), index.html:10249-10263 (grid isShared uses mealsMatch), index.html:10226-10227 (grid red includes EAT_OUT flag), index.html:3262-3280 (_autoSharedCells excludes batches with leftovers)
- **Claim**: The comment claims 'Visual-state checks (mirror the grid's own render conditions)' but: (a) the grid's green shared glyph outside touched cells uses `_v76_mealsMatch` (getMealId equality, L10251/L10263), while `_schedCellShared` uses SHARED_SCHEDULE + `_autoSharedCells` only. A shared cook WITH leftover portions (him+her same dinner + a later leftover — the standard Big Cook shape) sets `hasNonAnchorCell=true` so `_autoSharedCells` is NOT populated (L3262-3280, the batch becomes an auto-link group instead) — the cell shows a green ⚭ glyph but the menu's 'Shared meal' row is UNCHECKED. Tapping it (user intent: un-share) computes allOn=false and turns shared ON — writing an explicit 'shared' plus finding #1's V70 pin-clears, the opposite of what the user asked. (b) the grid's red includes per-slot `EAT_OUT[k]` (L10226) while `_schedCellEO` reads SHARED_SCHEDULE only; a CUSTOMIZED eat-out whose eo-* state was removed (e.g. by menu Clear — V85's eo branch preserves customized EAT_OUT via _isEatOutCustomized, L3501-3503) stays red in the grid with an unchecked 'eats out' row, and no number of menu taps can clear the red (the off toggle's V70 always preserves customized entries).
- **Failure scenario**: Mon dinner: him+her both have chicken teriyaki, him also eats the leftover Tue lunch (green glyph + group band on Mon dinner). User opens the Mon/Dinner menu to un-share it: 'Shared meal' shows NO checkmark; tapping it sets SHARED_SCHEDULE='shared' and runs the V70 clears (finding 1) — the cell stays green and the user's action did the inverse of their intent; a second tap is needed to actually un-share. The old center-tap had the same blind spot (`wasVisuallyShared` also ignored mealsMatch-only cells) but showed no contradicting checkmark UI.

### [LOW] R15-PANTRY-3 — The '🏠 −X' hint claims an applied deduction in views where no deduction was applied (Him/Her-only views, and unit-mismatch / wrong-basis fallbacks)
- ✅ **FIXED V278 (2026-07-18)**: '🏠 −' renders only in the Both view when the unit basis resolved AND this trip actually got credit (`item.pantryAppliedFrac > 0`); the amount shown is the slice of the entry credited to THIS trip in the entry's own units. Him/Her views and every fallback show '🏠 have'.
- **Where**: index.html:8976 (hint prefix) with 8962-8963 (gross him/her labels) and 4873 (silent gross fallback)
- **Claim**: When PANTRY_SUBTRACT is on, buildShopItemHtml switches the hint to the minus form for EVERY view: `const pfx=PANTRY_SUBTRACT?'🏠 −':'🏠 have ';` (8976). But Him/Her-only views deliberately stay gross (their qtyText comes from hisQty/herQty at 8962-8963, never through _pantryNetLabel), and even the Both view silently falls back to the gross label when the pantry unit mismatches (4873: `if(pu && su && pu!==su) return shopQtyWithCount(dbKey, qtyObj);`). In all those cases the row shows a gross quantity next to a '−X' hint that reads as already-subtracted.
- **Failure scenario**: PANTRY_SUBTRACT on; pantry 'extra firm tofu' qty 1 unit 'container'. Both view: unit guard trips ('container' ≠ 'oz') → label stays gross '2 containers (16oz)' while the hint reads '🏠 −1 container' — the user reads 2 as net-of-their-container and buys 2, ending up with 3. Him-only view: label is gross by design, hint still shows '🏠 −…' — same misread. The one state the minus phrasing is truthful in (Both view, unit-compatible) is indistinguishable from the states where it lies.

### [LOW] R15-PANTRY-4 — Pantry qty:0 tombstones are never garbage-collected — PANTRY and every sync payload grow monotonically
- **Where**: index.html:17929-17933 (soft delete), 18650 (payload), no GC anywhere
- **Claim**: shelfDeletePantry soft-deletes by writing `{qty:0, unit, _ts}` so removals propagate — correct for LWW — but unlike EAT_OUT_DB, which has `_gcEatOutTombstones()` invoked at merge (19038), PANTRY has no tombstone GC at load, merge, or rollover. Every item ever tracked stays in localStorage, in every getSyncPayload (18650), and is re-walked by forcePushSync's ts-bump loop (19176) forever.
- **Failure scenario**: A household that tracks and clears a handful of pantry items weekly accumulates hundreds of qty:0 entries over months. Each one rides every gist push/pull and force-push ts-bump. No correctness break today, but it is unbounded growth in a payload that already carries 3 weeks of data, and any future code that iterates PANTRY without the qty>0 filter (the renderShelf filter at 17980 is currently the only one) will trip over stale names. The eatOutDB pattern (age-based GC once a tombstone is older than the sync horizon) applies directly.

### [LOW] R15-SETTINGS-2 — setPersonEnabled is reachable during schedule edit mode with no V145-class guard — the edit-mode snapshot/diff spans a person-enabled flip
- **Where**: index.html:5912-5926 (setPersonEnabled) vs 19107/19136/19201 (the V145 guards on sync ops)
- **Claim**: pushSync/forcePushSync/pullSync all refuse to run during _scheduleEditMode because mutating scheduling state under a pre-captured _scheduleEditSnapshot corrupts the Apply diff (V145). setPersonEnabled mutates state with comparable blast radius — it invalidates the leftover cache, re-renders meals, and (via the V244 solo grid) removes the disabled person's rows from the very grid whose snapshot was captured with both persons — yet it has no edit-mode guard, and the ⚙️ gear that hosts it is tappable while edit mode is armed. Uncertainty declared: I have not traced every applyScheduleEdit branch to prove a concrete corruption, but the hazard class is exactly the one V145 was added for, and the V253 ops' PERSON_ENABLED gates (3940-3941, 4026, 4048) only cover NEW taps, not a pre-flip snapshot being applied post-flip.
- **Failure scenario**: User arms schedule edit mode, makes cell edits touching Her, opens ⚙️, disables Her (confirm accepted; grid re-renders without her rows), returns to the grid and taps Apply. The diff was computed against a snapshot containing Her state the user can no longer see or verify; alternatively Undo restores Her-cell values into hidden rows. Either way the commit/undo operates on invisible state — the exact stale-snapshot class V145 blocks for sync. Cheap fix: same refusal message in setPersonEnabled (and arguably setTabPref) while _scheduleEditMode is true.

### [LOW] R15-SHELF-2 — Shelf pantry rows: backslash (and '__proto__') in a pantry name breaks the edit/delete handlers — entry becomes un-editable and un-deletable from the UI
- **Where**: index.html:17985 and 17987 (onclick escaping), 17912 (_shelfEsc), contrast 8967
- **Claim**: The onclick strings escape only single quotes: `_shelfEsc(k).replace(/'/g,"\\'")`. _shelfEsc (17912) handles &, <, " for HTML but nothing escapes backslashes for the JS string context. A name containing a backslash either silently changes the key (JS drops unknown escapes: 'a\bc' lookup misses PANTRY['a\bc']) or, for a trailing backslash, produces a SyntaxError (the \ escapes the closing quote). shelfSetQty/shelfDeletePantry then `if(!PANTRY[name]) return;` — silent no-op. The shop-item code already learned this lesson: 8967 does `.replace(/\\/g,'\\\\').replace(/'/g,"\\'")`. Separately, adding an item literally named __proto__ never stores an entry: `PANTRY[name]={qty,…}` (17918) triggers the __proto__ setter and silently swaps PANTRY's prototype instead — the item just never appears, with no error.
- **Failure scenario**: User adds pantry item 'back\slash brand rice' (pasted from a note). The row renders, but the ✕ button calls shelfDeletePantry('back\slash brand rice') → JS parses '\s' as plain 's' → key 'backslash brand rice' → not found → silent return. The qty input's onchange no-ops the same way. The entry is stuck: it renders, syncs to the partner phone, and cannot be edited or deleted from either device's UI (only a manual localStorage/remote edit removes it).

### [LOW] R15-SHELF-3 — Shelf inputs reference undefined CSS var --card (no fallback) — transparent input backgrounds; same dead-var class V246 explicitly cleaned up elsewhere
- **Where**: index.html:17985, 17991-17993
- **Claim**: All four Shelf tab inputs (qty editor, name/qty/unit add-row) use `background:var(--card)`. No `--card` custom property is defined anywhere in the file (grep finds only these four usages), so the declaration is invalid at computed-value time and the background resolves to transparent. V246's change log states the two partner-dialog/toast `var(--card,…)` references were fixed to `var(--bg-card)` because '--card' is a non-existent var; renderShelf reintroduces it, this time with no fallback at all.
- **Failure scenario**: On the shipped dark theme the inputs render with the panel's background bleeding through instead of the intended card surface (#222428) — visible as flat borderless-looking fields; on any future lighter surface or gradient behind the panel the inputs become visually broken. Fix is `var(--bg-card)` per V246.

### [LOW] R15-SHELF-4 — Shelf 'Open from last week' is self-referential when viewing Last week — _prevWeekKey() returns 'last' for activeWeek 'last'
- **Where**: index.html:13683-13685 (_prevWeekKey), 17941 (renderShelf call)
- **Claim**: `_prevWeekKey()` is `activeWeek === 'next' ? 'this' : 'last'` — for activeWeek==='last' it returns 'last', i.e. the viewed week's own key. renderShelf calls it unconditionally (17941), so on the Last-week pill the '🫙 Open from last week … (use in Mon–Wed cooking)' section computes carryover FROM the very week being displayed and presents it as prior inventory. The helper predates the Shelf (V180) and its other consumers run in randomize/shopping contexts where 'last' is rarely active, but the Shelf is a pure browsing surface where the Last pill + Shelf tab combination is one tap away (the week bar renders on the shelf tab, 18602).
- **Failure scenario**: User taps Last (amber 'View Only') then 🧺 Shelf to review how last week went. The 'Open from last week' section shows last week's OWN end-of-cook2 partial (e.g. 'marinara — 1.5 cup open (use in Mon–Wed cooking)') labeled as if it were inventory carried INTO that week from the week before — historically wrong data with actionable-sounding guidance. (The freezer/staples sections have the same week-pair skew: usage from 'last', carry from 'last'.)

### [LOW] R15-SYNC-2 — personEnabled merge triggers renderMeals BEFORE loadWeekState and the R14-C5 cache invalidation — a render from half-merged state inside mergeSyncData
- **Where**: index.html:18966-18973 (merge branch) → 5907-5910 (applyPersonPrefs → setPerson) → 5965 (renderMeals), vs 19057 (loadWeekState) and 19076 (invalidateLeftoverCache)
- **Claim**: When a pulled payload changes PERSON_ENABLED to a solo state, the merge branch calls applyPersonPrefs() (18972), which calls setPerson(solo) (5909), which calls renderMeals() (5965) — all while mergeSyncData is only ~60% done: weekData maps are merged but the SEL/OVERRIDES globals haven't been rebound (loadWeekState runs at 19057) and the balanced/leftover caches haven't been invalidated (the R14-C5 fix at 19070-19076 runs later). This violates the documented rule 'Mutation paths must invalidate before renderMeals; sync merge is a mutation path' (19074-19075). Today it is rescued because pullSync/pushSync call renderMeals again after the merge completes in the same synchronous tick, so the browser paints only the final render — but the mid-merge render executes the full card pipeline against stale-SEL + stale-cache + new-PERSON_ENABLED state, and any exception it throws aborts mergeSyncData before loadWeekState/invalidateLeftoverCache/saveWeeks (19057-19078), leaving pantry saved (18952) but week state unsaved and caches stale, with the pull reporting a generic failure.
- **Failure scenario**: Partner phone disables Her and syncs; this phone (viewing the Her tab) pulls. Mid-merge, setPerson('him') renders meals from the not-yet-rebound SEL and un-invalidated caches — computing balanced ingredients for a week state that is half old, half new. If that render throws on the inconsistent intermediate (the class of state R14-C5 documented as dangerous), the user gets 'Pull failed: …', weekData merges are unsaved, and the app sits on stale caches. Safe ordering: defer applyPersonPrefs to after line 19076 (mirroring where rebindAdjTargets-style hooks run), or strip the setPerson/renderMeals side effects from the merge path.

### [LOW] SOLO-7 — randomizeWeek captures non-target freeze pins with the UNCOERCED target — a 'shared'/disabled-target call in solo destroys the disabled person's durable freeze pins
- **Where**: /Users/chris/Developer/Meal Planner/index.html:15706-15708 (also 15756-15758, 4214-4215, 4193-4205)
- **Claim**: `_captureNonTargetFreezePins(target||person)` (15706) and the first `_restorePreservedPins` (15708) run BEFORE the V242 solo coercion (15756-15758). If randomizeWeek is invoked with target='shared' (or the disabled person) while solo, capture returns null (4215: `if(!randTarget || randTarget === 'shared') return null;`) yet the run executes as a single-person randomize after coercion — so the unscoped `_clearFreezeOverrides()` (15707 and per-retry 15798) deletes the disabled person's `_freezePin` overrides (4199: `if(o._freezePin === true) return false;`) and nothing restores them. Their previously boundary-frozen amounts silently revert, surfacing as package waste when re-enabled. UI paths can't produce this today (popup gates buttons at 14267-14268 and applyScheduleEdit's rTarget=person is coerced by applyPersonPrefs 5907-5910), but the harness/console paths call `randomizeWeek('shared', …)` directly (e.g. 20242, 23279, 26317), and the coercion comment claims this is 'the single choke point' when it is not for pin capture.
- **Failure scenario**: Solo-her device; him's plan (preserved) carries _freezePin overrides keeping his batch totals on container boundaries. A stress run or console call executes `randomizeWeek('shared', seed)` — solo-coerced to 'her'. Capture returned null, so every retry's _clearFreezeOverrides strips him's freeze pins permanently. Him is re-enabled: his amounts have reverted to unfrozen values, trip totals sit off package boundaries, and INV20/shopping report waste that the last committed state had already eliminated — a polluted baseline for the next harness comparison as well.

### [LOW] SOLO-8 — setSchedule's V82 stale-'shared' auto-repair is dead in solo mode, and the shared/eo branches keep writing state onto the disabled person
- **Where**: /Users/chris/Developer/Meal Planner/index.html:14817-14827 (also 14851-14857, 14871-14873)
- **Claim**: The V82 repair that clears stale 'shared' cells requires BOTH meal ids non-null (14818-14820); with one person disabled getMealId always returns null for them, so stale 'shared' entries (which nothing cleans at disable time) can never self-repair while solo — and the menu's 'Shared meal' toggle is hidden in solo (4006), so the only per-cell cleanup is Clear. Meanwhile every Apply re-stamps MANUAL_SET on the DISABLED person's keys for 'shared' (14826-14827) and 'eo-her'/'eo-both' cells (14798-14800), deletes both sides' SKIPPED/EAT_OUT on shared cells (14851-14857), and re-creates EAT_OUT placeholders for the disabled person (14871-14873). Most stamps on untouched cells are reverted by the temp-lock cleanup (14720), but cells whose disabled-side touched flag was stamped by _schedOpClear/_schedOpShared (3887-3888, 3859-3860) are exempt from temp-locking, letting the MANUAL_SET write persist on the disabled person's slot.
- **Failure scenario**: Solo-him with the default 'shared' intent still on all seven dinner cells. User taps Clear on Monday dinner via the cell menu (stamping her touched-side at 3888), then Apply. For the other six dinners the stale 'shared' survives forever (repair dead), keeps the cells reporting shared in the edit-mode menu checkmarks, and each Apply churns MANUAL_SET/EAT_OUT writes onto the disabled person's keys — slot-ts bumps that sync outward and, for touched-side-stamped cells, a persistent MANUAL_SET the user never set, which on re-enable locks her slot from the next randomize.

### [LOW] SOLO-9 — Shopping tab person picker not solo-gated — a dead 'Her only' view remains selectable while Her is disabled
- **Where**: /Users/chris/Developer/Meal Planner/index.html:18024-18026
- **Claim**: Unlike the meal person tabs (applyPersonPrefs, 5904-5906) and the randomize popup buttons (14267-14268), the Shopping tab's Him only / Her only / Both buttons render unconditionally, so in solo mode the disabled person's view is still offered. It shows an empty list (getMealId-gated) except for the disabled person's late-snack ingredients (SOLO-4), presenting a confusing 'Her only' list for a person the planner claims to have stopped shopping for.
- **Failure scenario**: Her is disabled. User opens Shopping, taps 'Her only' (still rendered), and sees either an empty list or only her orphaned late-snack items — reading as a bug ('where did her food go?') rather than the intended 'Her is disabled'. If shopPerson was already 'her' when the disable synced in, the tab silently opens on the dead view.

### [LOW] c6-delete-auto-group-resurrects-via-defaults — Delete on an auto group (now prominently reachable via the V253 'Linked groups — tap to edit' submenu) deletes SEL but the DEFAULTS fallback can re-derive the identical group, so Delete appears to do nothing while silently wiping MANUAL_SET/USER_EDITED/OVERRIDES
- **Where**: index.html:14186-14212, index.html:3062-3067, index.html:4461-4468, index.html:3956-3965
- **Claim**: deleteCurrentGroup resolves the group via getLinkGroupForMeal, which returns AUTO groups too (L3062-3066). For an auto group it deletes MANUAL_SET/USER_EDITED/SEL + clearOverrides on every slot and splices LEFTOVER_LINKS (a no-op — the auto group isn't in there). getMealId falls back to DEFAULTS when SEL is empty (L4468), so if the batch geometry came from DEFAULTS meals, computeLeftovers re-derives the same batch on the very next render and the auto group reappears unchanged. Pre-existing behavior (the old picker's group rows had the same path), but V253's meal submenu lists auto groups as first-class 'tap to edit' rows on every cell menu, making this the default way users encounter Delete.
- **Failure scenario**: Fresh week where DEFAULTS put the same dinner meal on him+her Monday and him Tuesday (a DEFAULTS-derived auto group, no SEL entries). User: edit mode → Mon dinner cell → Pick meal… → taps the group row → Delete. deleteCurrentGroup deletes nothing visible (SEL was already empty), the next render re-derives the identical auto group from DEFAULTS, and the grid looks byte-identical — the user's Delete 'did nothing', except any ingredient pins (OVERRIDES) they had on those slots were destroyed.

### [LOW] c8-clean-verifications-informational — Informational — audit questions verified CLEAN: _paintSlot='menu' has zero value-readers; Cancel/togglePaintMode arm branch is safely dead; V147 week-switch teardown fully covers menu-armed paint; auto-group materialization/color parity holds
- **Where**: index.html:13945, index.html:3927, index.html:13970-13972, index.html:18541-18549, index.html:10442, index.html:14131-14133
- **Claim**: Not a defect — verification results the parent asked for. (1) _paintSlot: every reference in the file is a write or a clear (writes: L3927 'menu', L13973 picker value, harness L23888/24047 'main'; clears: L13967/14141/14191/14206/18548/25618/25821). No code path reads its value — paintAccept, the grid render, the paint bar, and the harness checks never consult it, so 'menu' vs the old category strings is inert. (2) togglePaintMode's arm branch is unreachable-but-safe: its only UI caller is the paint-bar Cancel button (rendered only when _paintMode=true → toggle-OFF branch), and the arm branch null-checks the removed picker elements (`const slotSel=document.getElementById('paintSlotSel'); ... if(!slotSel||!mealSel||!slotSel.value||!mealSel.value) return;` L13970-13972) so an accidental call no-ops. onPaintSlotChange/onPaintMealChange have zero callers. (3) V147 switchWeek teardown (L18541-18549) clears all five globals menu-armed paint uses (_paintMode/_paintQueued/_paintMealId/_paintSlot/_paintExistingColorIdx); _armPaintWithMeal retains no extra state (menuCtx is not stored) and the menu overlay is a fixed inset-0 modal (L391) so switchWeek can't fire while it's open. (4) _paintExistingColorIdx parity: _armPaintWithMeal (L3931-3937) mirrors togglePaintMode's V75 capture exactly, the paint-preview allocator (L10442) and paintAccept's reuse (L14131-14133) both read it, and auto-group materialization keeps the auto color with no allocator collision (the auto group vanishes next render once its slots go manual).
- **Failure scenario**: None — recorded so the verification wave doesn't re-derive these; the only actionable residue in the dead-kept surface is c7's stale key format.

### [LOW] l1-movepending-survives-randomize-and-sync — Move mode stays armed across randomize / sync pull / schedule Apply — swap completes against re-rolled meals and locks them
- **Where**: index.html:10953-10976 (_movePending lifecycle), index.html:15784+ (randomizeWeek has no disarm), index.html:11006-11008 (MANUAL_SET stamp)
- **Claim**: Nothing in randomizeWeek/runRandomizeFromPopup, mergeSyncData, or applyScheduleEdit clears _movePending (grep of the file shows _movePending is only referenced at 9954-9961, 10953-10976, and the harness). After any of these re-roll SEL, the armed {p,d,s} still points at the same slot keys, now holding different meals; the Moving…✕ / ⤓ Here pills re-render against the NEW plan.
- **Failure scenario**: User arms move on Monday dinner ("chicken curry"), then taps Create Plan in the Schedule header. The randomizer replaces the whole week; move mode is still armed. User (or a stray tap) hits ⤓ Here on a destination card and swaps two freshly-rolled meals they never inspected — both slots become MANUAL_SET+USER_EDITED, permanently excluded from subsequent randomizes and shown with Set chips, from what the user perceived as leftover UI state. State stays internally consistent (swap reads current SEL), but the intent is stale and the randomize-lock side effect is silent.

### [LOW] l2-solo-mode-disable-strands-armed-move — Disabling the source person (V242 solo mode) mid-arm strands move mode with no reachable cancel until reload
- **Where**: index.html:4461-4468 (getMealId PERSON_ENABLED gate), index.html:10963-10965 (_moveEligible src meal check), index.html:5904-5921 (setPersonEnabled — no disarm)
- **Claim**: setPersonEnabled does not clear _movePending. If the armed source belongs to the person being disabled, getMealId(src.p,...) returns null (4467) so _moveEligible's srcMeal check (10963-10965) fails for every destination — no ⤓ Here renders anywhere — while the Moving…✕ pill lives on a card in the now-hidden ptab. Move mode is armed with zero visible affordances; since _movePending is a non-persisted module var, only a page reload (or re-enabling the person and finding the card) clears it.
- **Failure scenario**: User arms ⇄ on a Him card, opens ⚙️ and unchecks Him (testing solo mode for Her). Him's tab disappears; no card anywhere shows a move pill; move mode is silently armed for the rest of the session. Re-enabling Him restores the Moving…✕ pill — but the user has no indication anything was armed, and (combined with l1) later interactions can complete a swap they forgot they started.

### [LOW] l3-freeze-pins-travel-cross-trip — Freeze pins (scalable:false, non-_userSet) travel verbatim to a slot in the other trip — both cook1 and cook2 totals leave container boundaries and the stale pin is re-applied on every cache rebuild until the next randomize
- **Where**: index.html:10990-10993 (overrides travel), index.html:11009 (leftover cache only nulled), CLAUDE.md 'Package Waste Elimination' (freeze pin semantics)
- **Claim**: swapMeals moves the whole OVERRIDES object — including freezeTripTotals' scalable:false pins that were sized so the OLD trip's ROUND_PKG/PRODUCE totals landed on container boundaries. A Mon(cook1)↔Fri(cook2) swap moves the pinned per-serving amount into the other trip: the donor trip loses a frozen contribution and the recipient gains one sized for a different boundary equation, so BOTH trips' totals go off-boundary. Because the pins persist in OVERRIDES, any later cache rebuild (skip toggle anywhere → invalidateLeftoverCache → applyOverrides) re-applies the stale amounts rather than letting the adjuster re-fit; on a single-person randomize of the OTHER person, _captureNonTargetFreezePins/_restorePreservedPins re-applies swapped-in stale pins too. Self-heals only at the next full randomize of the pin's person (_clearFreezeOverrides). Per the V243 design stance manual rearrangement is the user's call and the shopping list reports the waste honestly — but the design comment promises only 'no pipeline re-run', not multi-day persistence of geometry-stale pins across rebuilds.
- **Failure scenario**: Post-randomize, marinara is frozen so cook1 totals exactly 1 jar (pins on Mon dinner) and cook2 totals 1 jar (pins on Fri dinner). User swaps Mon dinner ↔ Fri dinner. Shopping now shows cook1 and cook2 each with fractional-jar demand and ⚠️ waste warnings; two days later the user toggles a snack skip (full cache rebuild) and the stale pinned amounts are re-applied verbatim at the wrong-trip slots, cementing the off-boundary geometry until the next Create Plan.

### [LOW] l4-manual-link-group-does-not-follow-meal — Manual leftoverLinks group does not follow a moved meal — validator drops the source slot, never adds the destination; a 1-slot group persists and holds its palette color
- **Where**: index.html:3178-3196 (_validateManualLinkGroups), index.html:10100 (runs in renderMeals), index.html:11010 (swapMeals → renderMeals)
- **Claim**: Verified: the self-heal DOES run (renderMeals line 10100, invoked by swapMeals line 11010) and it DROPS rather than corrupts — g.slots is filtered to entries whose SEL still equals g.meal (3190), the group is deleted only when 0 slots remain (3193), otherwise shrunk (3194). Consequences: (a) the moved meal's new slot is never added to its old group, so a user-painted cook+leftover chain silently loses the moved member while the detector may now derive a completely different batch geometry for it; (b) a group shrunk to 1 slot keeps rendering its band and keeps one of the 8 strict-no-reuse palette colors allocated (V73 allocator returns -1 at saturation), so repeated swaps can exhaust paintable colors with orphan 1-slot groups. Auto-groups rebuild fine (transient).
- **Failure scenario**: User painted a manual group [him Mon dinner (cook), him Tue dinner, him Wed dinner] for meal M, then uses ⇄ to move Wed's portion to Friday. The group silently becomes [Mon, Tue]; Friday's M is unlinked (detector puts it in a separate batch — Fri is outside Mon+2); the schedule grid shows the painted chain ending Tuesday while the meal actually recurs Friday. Do this on several groups and the 8-color palette saturates with shrunken remnants, blocking new Link leftovers… paints (allocator -1 → amber fallback).

### [LOW] l5-no-meal-slot-compat-check — _moveEligible never checks m.slots — a custom lunch-only meal swapped into a dinner slot leaves the meal-select showing the wrong meal
- **Where**: index.html:10955-10967 (_moveEligible), index.html:9733/9747 (slotMeals filter), index.html:9380-9385 (no selected option fallback)
- **Claim**: Eligibility checks only the slot GROUP (_slotTypeGroup: lunch/dinner interchangeable) and person availability — never srcMeal.slots.indexOf(dstS). All 40 shipped mains declare slots:['lunch','dinner'], but the Recipes tab lets users create a lunch-only or dinner-only custom main. Swapping such a meal across the lunch/dinner boundary puts it in a slot whose card builds its <select> from getMealsForSlot(s) (which excludes it), so no <option> carries selected — the browser displays the FIRST option's label while the card header/macros show the real meal. Any subsequent nudge of that select fires changeMeal to whatever meal happens to be listed first.
- **Failure scenario**: User creates custom recipe 'Big Salad' with only the Lunch slot chip, assigns it Monday lunch, then ⇄-moves it to Tuesday dinner (eligible: same 'main' group). Tuesday's dinner card header says 'Big Salad' but its dropdown displays the alphabetically-first dinner meal as if selected; tapping the dropdown and dismissing (or any change event) replaces Big Salad with that unrelated meal.

### [LOW] l6-swap-does-not-clear-eo-schedule-state — swapMeals dissolves only 'shared' SHARED_SCHEDULE state — unlike changeMeal it never clears eo-him/eo-her/eo-both, leaving an INV24-adjacent contradiction reachable from stale sync state
- **Where**: index.html:11000-11005 (swapMeals shared-only dissolve) vs index.html:11037-11041 (changeMeal eo-* clearing)
- **Claim**: changeMeal — the stated behavioral reference for manual meal picks — clears the person's eat-out schedule state when they pick a meal ('they're now eating a home meal'). swapMeals mirrors only the 'shared' dissolution. Normally an eo-* cell also carries an EAT_OUT slot entry (V70 cascade) which blocks eligibility at 10961 — but INV24 exists precisely because sync merges (per-key LWW on sharedSchedule vs eatOut independently, saveWeeks 18456-18459) can strand SHARED_SCHEDULE='eo-him' without the matching EAT_OUT entry. In that state a swap into/out of him's slot succeeds and leaves the grid claiming he eats out while SEL/MANUAL_SET/USER_EDITED say he eats a manually-set meal — the contradiction changeMeal repairs.
- **Failure scenario**: Device B pushes SHARED_SCHEDULE['Monday_dinner']='eo-him' but the EAT_OUT[him_Monday_dinner] key loses the per-key LWW to a newer local delete (the stale class INV24 was built for). On device A the him Monday dinner card renders normally (EAT_OUT absent), user ⇄-swaps a meal into it. The schedule grid keeps painting him's Monday dinner as eat-out while the card shows a Set meal; next verifyInvariants run won't flag it (INV24 checks eo∧SKIPPED, not eo∧manual-meal), so the contradiction persists silently.

### [LOW] l7-user-swap-can-bake-permanent-hard-inv11 — A swap among MANUAL_SET slots can create same-meal batch geometry that makes hard INV11 fire on every future randomize — checker has no manual-action exemption
- **Where**: index.html:7894-7932 (INV11 checker), index.html:11006 (swap stamps MANUAL_SET), index.html:10943-10952 (design comment)
- **Claim**: The V243 comment declares 'invariants judge randomizer output, not user rearrangement — a user-made same-meal adjacency is their call', but the INV11 checker iterates ALL anchors with no MANUAL_SET/USER_EDITED exemption. If a user swap (plus pre-existing manual/painted slots) yields two batches of the same meal with gap_days<2 across slots that are all MANUAL_SET, Phase 1 preserves them on every randomize, the retry selector can never reach inv11=0, and verifyInvariants emits a hard INV11 violation on every Create Plan — a recurring hard-INV fire that, per the repo's own 'any hard-INV fire is a bug, must be investigated' policy, will burn a future investigation on user-created geometry the checker can't distinguish from a randomizer bug.
- **Failure scenario**: User has a painted manual group: meal M on Mon/Tue/Wed dinner (all MANUAL_SET, one 3-portion batch, window Mon+2=Wed). They ⇄-swap M into Thursday dinner from elsewhere (Thu now MANUAL_SET too). Detector: Mon-batch ends Wed, Thu is a new anchor, gap_days=0. Every subsequent randomize preserves all four slots and reports 'INV11 gap-too-small' — a permanent hard-invariant console fire with no randomizer defect behind it and no way for the user to know why.

### [LOW] l8-harness-movemeal-coverage-gap — Harness moveMealOp deliberately tests only singleton same-person swaps — every audited interaction class (batch members, anchors, cross-person, link groups, identical meals, cross-trip pins) is unexercised
- **Where**: index.html:23129-23167 (moveMealOp)
- **Claim**: The Slot Mod harness restricts source and destination to meals occurring exactly ONCE in the week, same person, non-MANUAL_SET, lunch/dinner only — explicitly 'so the swap can't create batch-adjacency interactions'. That means the exact code paths where V243 interacts with the detector, unify amounts, link groups, freeze pins, and the shared-cell dissolution (findings m3, l3, l4 above) have zero regression coverage; the 265/265 Slot-Mod pass claimed for V243 does not attest to them.
- **Failure scenario**: Any regression introduced in the batch/cross-person/link-group arms of swapMeals (e.g. a future refactor of the cache exchange or shared-dissolve loop) passes the full Slot Mod suite green while breaking the shipped feature for exactly the swaps users perform on real weeks (which are dominated by batch meals, not singletons).

### [LOW] l9-verbatim-rows-dont-survive-rebuild — The 'verbatim rows' promise silently reverts on any later cache rebuild — moved meals re-adjust to the destination slot's budget with no user action
- **Where**: index.html:10994-10997 (cache exchange), index.html:6109-6111 (rebuild path), index.html:4603 (invalidateLeftoverCache wipes day cache)
- **Claim**: swapMeals' design comment promises the user 'sees the result of just my change' via verbatim balanced-cache rows, but those rows live only in the in-memory _dayBalancedCache. Nothing pins non-_userSet amounts, so the first full invalidation after the swap — page reload, sync pull (R14-C5 invalidates), any skip/eat-out toggle (10675/10834 call invalidateLeftoverCache), changeMeal on any slot — rebuilds the swapped slots from SEL+OVERRIDES through adjustIngredients against the DESTINATION slot's budget and the NEW detector geometry. Amounts the user just verified visibly change later with no action of theirs. This is swap-specific: for changeMeal the rebuild path is the display path from the start, so nothing shifts afterward.
- **Failure scenario**: User swaps his 900-kcal Friday dinner into Monday LUNCH (lunch budget ≈ 0.35×(2800−shake) ≈ 800 kcal, outside the min(15%,80) skip-if-close band). The card shows 900 kcal — exactly what he approved. That evening he skips a snack (full cache invalidation); Monday lunch silently rebuilds to ~800 kcal with rescaled ingredients. The meal he verified after the move is not the meal on the card the next time he looks, and no Set/pin explains the change.

### [LOW] v253-eo-off-keeps-link-visual — _schedOpEO(off) does not stamp _scheduleEditClearedLinkSides for a side that also carries a link band (V85's eo branch did) — deliberate-looking divergence, reported for completeness
- **Where**: index.html:3870-3875 (off branch), index.html:3584-3586 (old V85 eo branch stamps link-clear)
- **Claim**: Old flow: one tap on an eo side that was also in a link group cleared the eo AND suppressed the link visual (`if(hasLink) _scheduleEditClearedLinkSides[sk_this] = true;` L3586, per the V85 every-first-tap-clears contract). The menu's directional 'eats out → off' clears only the eo layer; the link band re-appears. Under the menu's targeted-toggle semantics this is arguably MORE correct (un-eating-out shouldn't also un-link), but it is a write-set divergence from the handlers the ops claim to mirror exactly, and it changes the Apply outcome: the slot keeps its link-group membership where the old gesture's stamp + touched flag led to a reroll that dropped it via _validateManualLinkGroups.
- **Failure scenario**: Him's Thu lunch is a link-group leftover AND was set eat-out. User unchecks 'Him eats out' in the menu: the link band stays (new) vs old behavior where the band cleared and the slot left the group after Apply. If the mirror-exactly contract matters for the Slot Mod harness comparisons, this is a documented divergence to either bless or fix.

### [LOW] v253-eo-on-skipped-side — _schedOpEO(on=true) on a pill-skipped side leaves SKIPPED set — the cell renders skip-grey instead of eat-out red mid-edit, and the old flow's preserveSel stamp is lost
- **Where**: index.html:3862-3869 (no SKIPPED handling), index.html:3590-3597 (old V85 skip branch), index.html:10224-10228 (skip beats eat-out in render)
- **Claim**: The on-branch of _schedOpEO never touches SKIPPED. Old flow: tapping toward eo on a skipped side first hit V85's skip branch (`delete SKIPPED[sk_this]; ... _scheduleEditPreserveSel[sk_this] = true;` L3590-3597), then the second tap set eo. Menu flow: SHARED becomes 'eo-him' while SKIPPED[him_k] stays true. Mid-edit this is INV24's eo∧skip contradiction pair (resolved only at Apply by _syncEatOutsToSharedSchedule L5667-5669, and never checked mid-edit since verifyInvariants runs post-randomize) and the grid renders the SKIP grey because `himEatOut=!himSkippedSlot && …` (L10226) — the user taps 'Him eats out' and the cell visibly stays grey skip, not red. Additionally the preserveSel stamp the old flow made is absent, so the touched slot is re-rolled at Apply instead of keeping its SEL.
- **Failure scenario**: Him's Wed lunch is Skipped via the card pill. Edit mode → cell menu → 'Him eats out'. The cell stays grey (skip visual wins over red) — looks like the action failed; user taps again, which now toggles eo OFF (allOn2 was true), compounding confusion. If they Apply while eo is on, sync clears the skip and the eat-out lands, but the slot's SEL is re-rolled (touched, no preserveSel) where the old two-tap path preserved it.

### [LOW] v253-link-prequeue-mixed-cell — 'Link leftovers…' on a cell where him and her have different meals pre-queues BOTH halves for him's meal — Assign silently overwrites her's meal
- **Where**: index.html:3910-3917 (schedMenuLink him-first), index.html:3938-3941 (pre-queue both persons), index.html:14045-14051 (paintAccept overwrites SEL)
- **Claim**: `schedMenuLink` resolves the cell's meal him-first (`getMealId('him',m.d,m.s)||getMealId('her',m.d,m.s)`, L3915). When that meal has no existing group, `_armPaintWithMeal` pre-queues the tapped cell for BOTH enabled persons (L3940-3941) regardless of whether her's half actually holds that meal. If the user taps Assign without noticing, paintAccept writes `SEL[q.key]=_paintMealId` + MANUAL_SET + clearOverrides for her slot (L14046-14051) — her different meal and its overrides are replaced by him's meal. The old picker flow never pre-queued anything, so this silent cross-person overwrite path is new to V253/V253's _armPaintWithMeal.
- **Failure scenario**: Wed dinner: him=salmon stir fry, her=miso tofu (her's chosen deliberately). User taps the cell → 'Link leftovers…' (intending to chain HIS salmon to Thu lunch), then taps Thu-lunch-him and Assign. Because her Wed half was silently pre-queued, her miso tofu is replaced by salmon and her slot's ingredient overrides are cleared — a meal change she never requested, visible only if she checks her tab.

### [LOW] v253-menu-html-injection-labels — _schedMenuMealListHtml interpolates meal labels and protein-group names into innerHTML unescaped — user-created names containing markup break or script the menu
- **Where**: index.html:3964, index.html:3980-3982 (raw label/section injection), index.html:19351 (ids are slug-safe)
- **Claim**: Group rows (`_smRow('schedMenuChooseMeal(\''+g.meal+'\')','rgba('+rgb+',1)',meal.label,false)`, L3964) and meal rows (`html+='<div class="smenu-row" ... onclick="...schedMenuChooseMeal(\''+mm.id+'\')">'+mm.label+'</div>'`, L3982) plus section headers (`'<div class="smenu-sec">'+pk+'</div>'`, L3980 — pk comes from getPrimaryProtein, i.e. ingredient names incl. user-entered custom-ingredient names) insert free text into insertAdjacentHTML with no escaping. Meal IDs in the onclick strings are safe only by construction (customMealId slugs to `[a-z0-9_]`: L19351 `'custom_'+label.toLowerCase().replace(/[^a-z0-9]+/g,'_')...`) — there is no escaping layer, just a lucky slug. Labels are NOT slugged: a custom recipe named with `<`/`<img onerror=…>` renders/executes in the menu (self-XSS on own device, and labels sync to the partner's device via customMeals). Same class of hole pre-exists in buildMealSelectOpts option labels (L9380-9386), so V253 widened an existing surface rather than introducing it; day/slot strings are constants and safe.
- **Failure scenario**: User creates a recipe named `Mom's <b>Best</b> Chicken` in the New Recipe form. Opening any cell menu → 'Pick meal…' renders bold-injected markup; a label like `x<img src=x onerror=alert(1)>` executes on menu open — including on the PARTNER's phone after customMeals sync. At minimum the row's visible text is corrupted; unbalanced tags can break the remaining rows of the menu.

### [LOW] v253-menu-lifecycle-no-editmode-guard — Menu action handlers never re-check _scheduleEditMode, and nothing closes the overlay on edit-mode exit; schedMenuChooseMeal also lacks schedMenuLink's null-menu guard
- **Where**: index.html:3890-3905 (schedMenuAction), index.html:3816-3820 (closeSchedMenu is the only remover), index.html:3919-3923 vs 3913-3914 (guard asymmetry)
- **Claim**: openSchedMenu gates on `_scheduleEditMode` (L3810), but schedMenuAction/schedMenuPickMeal/schedMenuLink/schedMenuChooseMeal do not, and the overlay lives on document.body so renderMeals never removes it; enterScheduleEditMode/applyScheduleEdit/undoScheduleEdit/clearScheduleEdit never call closeSchedMenu. In the current UI this is unreachable-in-practice because the fixed z-400 backdrop (`.smenu-backdrop{position:fixed;inset:0;...z-index:400}` L391) covers Apply/Undo/week pills/tabs — I found no auto-sync timer or other programmatic edit-mode exit. But any future or harness-driven path that exits edit mode (MPStress calls applyScheduleEdit/undoScheduleEdit directly, e.g. L23621+) while an overlay exists leaves a live menu whose taps then execute _schedOp* OUTSIDE edit mode — writing SHARED_SCHEDULE plus touched/preserveSel trackers that are consumed by the NEXT edit session's Apply diff (stale-intent rerolls), violating the V25 'grid editing is edit-mode-only' contract. Separately, `schedMenuChooseMeal` omits the `if(!m) return;` guard `schedMenuLink` has, so with a null `_schedMenu` it still arms paint mode with nothing queued.
- **Failure scenario**: A test/harness sequence (or future feature, e.g. a sync-pull handler that exits edit mode) calls undoScheduleEdit while a cell menu is open. The orphaned overlay remains; the user taps 'Shared meal'. _schedOpShared writes SHARED_SCHEDULE + _scheduleEditTouchedSides outside any edit session; the user later enters edit mode (trackers reset — but the SHARED_SCHEDULE write already leaked past Undo) — state mutated outside the sandbox the edit-mode contract promises. Cheap hardening: guard schedMenuAction on _scheduleEditMode and call closeSchedMenu() from the four edit-mode entry/exit functions.

---

# Chunk 3/4 — primary-agent self-audit (cards CSS, grid render, drift, docs)

LOW R15-DEAD-1: `.sched-pill-mid` CSS rule dead (V254 removed the overlay; comment-only ref).
LOW R15-DEAD-2: `.sched-zone/-l/-c/-r` CSS rules dead (V253 header menus replaced preset zones).
LOW R15-DEAD-3: `SKIP_BDR` computed per cell with zero consumers since V257 borderless.
LOW R15-DEAD-4: dead picker cluster kept deliberately — onPaintSlotChange/onPaintMealChange +
  togglePaintMode arm branch + _paintSavedSlot/_paintSavedMeal + paintSlotSel/paintMealSel reads
  (zero callers; onPaintSlotChange would NPE if ever invoked without the picker DOM).
LOW R15-DEAD-5: paintToggleAll/Him/Her stale-key trio (≡ chunk-1 c7).
LOW R15-DEAD-6: `.leftover-badge` CSS (known dead since V239, still present).
LOW R15-NAME-1: himShowSetDot/herShowSetDot names stale (drive the V271 arcs now) — cosmetic.
LOW R15-DOC-1: CLAUDE.md "Schedule Edit Mode" lacks the V254–V274 visual-language summary
  (chip/arcs/washes live only in Current State bullets).
CLEAN (verified): _escTip sufficient for double-quoted data-tip attrs; default neutral tile applied
  AFTER gradient conversion (stays flat); ACC_RGB covers all pushed accStates; lGroupRgb null guarded;
  `_hasGreenSharedAnyCell` harness probe matches the V260 glyph rgba form; accCls derives from the
  same variables as the pills (no drift by construction); INV9/23/25 do not read the new CSS.
